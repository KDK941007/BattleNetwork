(()=>{
  const RUNTIME=window.BattleNetworkEnemy1Runtime,FIELD=window.BattleNetworkField,ENEMY=window.BattleNetworkEnemy,battle=document.getElementById('battle'),scene=document.getElementById('scene');
  if(!RUNTIME||!FIELD||!ENEMY||!battle||!scene)throw new Error('BattleNetworkEnemy1PatternTestUI: required dependency is missing.');
  const PX=.72,PY=.36,SW=FIELD.WORLD_SIZE*PX*2,SH=FIELD.WORLD_SIZE*PY*2;
  const wrap=document.createElement('div'),toggle=document.createElement('button'),tools=document.createElement('div'),patternButton=document.createElement('button'),rangeButton=document.createElement('button'),glowButton=document.createElement('button'),detail=document.createElement('span');
  wrap.dataset.testOnly='enemy-debug-tools';
  wrap.style.cssText='position:absolute;right:10px;top:10px;z-index:40;display:flex;align-items:center;gap:6px;padding:5px 7px;border:1px solid rgba(255,255,255,.5);border-radius:8px;background:rgba(8,12,20,.82);color:#fff;font:700 11px/1.2 system-ui,sans-serif;pointer-events:auto;';
  [toggle,patternButton,rangeButton,glowButton].forEach(button=>{button.type='button';button.style.cssText='min-height:32px;border:1px solid #ffe27a;border-radius:6px;background:#30270d;color:#fff7c9;font-weight:900;font-variant-numeric:tabular-nums;padding:4px 8px;'});
  toggle.style.minWidth='82px';patternButton.style.minWidth='92px';tools.style.cssText='display:flex;align-items:center;gap:6px;';detail.style.cssText='white-space:nowrap;font-variant-numeric:tabular-nums;';
  const svg=document.createElementNS('http://www.w3.org/2000/svg','svg');
  svg.dataset.testOnly='enemy-perception-ranges';svg.setAttribute('viewBox',`0 0 ${SW} ${SH}`);svg.style.cssText=`position:absolute;left:0;top:0;width:${SW}px;height:${SH}px;overflow:visible;pointer-events:none;z-index:6;`;
  scene.appendChild(svg);
  const rings=new Map();
  function sec(ms){return `${(ms/1000).toFixed(2)}s`}
  function project(x,y){return{x:(x-y)*PX+SW/2,y:(x+y)*PY}}
  function circlePath(cx,cy,radius){let d='';const steps=48;for(let i=0;i<=steps;i++){const a=i/steps*Math.PI*2,p=project(cx+Math.cos(a)*radius,cy+Math.sin(a)*radius);d+=(i?'L':'M')+p.x.toFixed(1)+' '+p.y.toFixed(1)}return d+'Z'}
  function ensureRings(enemyId){let pair=rings.get(enemyId);if(pair)return pair;const start=document.createElementNS('http://www.w3.org/2000/svg','path'),release=document.createElementNS('http://www.w3.org/2000/svg','path');start.setAttribute('fill','rgba(255,211,82,.07)');start.setAttribute('stroke','rgba(255,211,82,.95)');start.setAttribute('stroke-width','5');start.setAttribute('stroke-dasharray','16 10');release.setAttribute('fill','none');release.setAttribute('stroke','rgba(84,235,255,.9)');release.setAttribute('stroke-width','4');release.setAttribute('stroke-dasharray','9 12');svg.append(start,release);pair={start,release};rings.set(enemyId,pair);return pair}
  function clearStale(activeIds){for(const [id,pair] of rings){if(activeIds.has(id))continue;pair.start.remove();pair.release.remove();rings.delete(id)}}
  function renderRanges(){const debug=RUNTIME.getDebugState();if(!debug.enabled||!debug.showPerception){svg.style.display='none';return}svg.style.display='block';const config=RUNTIME.getEnemyConfig(),startRadius=FIELD.toWorldDistance(config.perceptionStartTiles),releaseRadius=FIELD.toWorldDistance(config.perceptionReleaseTiles),activeIds=new Set();for(const enemy of ENEMY.getActiveEnemies()){activeIds.add(enemy.id);const pair=ensureRings(enemy.id);pair.start.setAttribute('d',circlePath(enemy.x,enemy.y,startRadius));pair.release.setAttribute('d',circlePath(enemy.x,enemy.y,releaseRadius));pair.start.setAttribute('opacity',RUNTIME.getPerception(enemy.id)?'.55':'1');pair.release.setAttribute('opacity',RUNTIME.getPerception(enemy.id)?'1':'.5')}clearStale(activeIds)}
  function render(){const debug=RUNTIME.getDebugState(),p=RUNTIME.getPattern(),config=RUNTIME.getEnemyConfig();toggle.textContent=`TEST ${debug.enabled?'ON':'OFF'}`;toggle.style.background=debug.enabled?'#14532d':'#30270d';tools.style.display=debug.enabled?'flex':'none';rangeButton.textContent=`知覚 ${debug.showPerception?'ON':'OFF'}`;glowButton.textContent=`発光 ${debug.showAttackGlow?'ON':'OFF'}`;patternButton.textContent=`PATTERN ${p.id}`;detail.textContent=`知覚 ${config.perceptionStartTiles} / 解除 ${config.perceptionReleaseTiles} / FS ${sec(p.fullSyncWindowMs)}`;if(!debug.enabled)svg.style.display='none'}
  toggle.addEventListener('click',()=>RUNTIME.setDebugEnabled(!RUNTIME.getDebugState().enabled));rangeButton.addEventListener('click',()=>RUNTIME.setDebugOption('showPerception',!RUNTIME.getDebugState().showPerception));glowButton.addEventListener('click',()=>RUNTIME.setDebugOption('showAttackGlow',!RUNTIME.getDebugState().showAttackGlow));patternButton.addEventListener('click',()=>{RUNTIME.cyclePattern();render()});
  RUNTIME.subscribeDebug(render);tools.append(patternButton,rangeButton,glowButton,detail);wrap.append(toggle,tools);battle.appendChild(wrap);
  function frame(){renderRanges();requestAnimationFrame(frame)}requestAnimationFrame(frame);
})();
