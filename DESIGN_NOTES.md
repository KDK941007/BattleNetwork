# BattleNetwork 設計メモ

## 開発方針

- ロックマンエグゼ4の描画・戦闘感をベースにしつつ、指定フィールド内を自由移動する360度アクション戦闘とする。
- ウィルスやボスはウェーブ形式で出現する。
- 現段階では基礎戦闘・操作感を優先し、キャラクターやステージの本格ビジュアル制作は後工程とする。
- 通常は `main` を直接更新する。ブランチを分ける必要がある場合は別途指示する。

## フィールド・カメラ

- 横画面を前提とする。
- フィールドは斜め上視点を考慮した正方形をベースとする。
- 現在のプロトタイプのフィールドサイズを基準とし、広さは実機確認しながら調整する。
- カメラは操作キャラクターを追従する。
- キャラクターはフィールド内を左スティックで360度自由移動する。

## コントローラー

- 左側：移動スティック。
- 右側：A / B / X / Y の4ボタン。
- A：バトルチップ使用。
- B：ロックバスター。
- X：緊急回避ダッシュ。
- Y：現時点では未定。将来機能用として確保する。
- 設定メニュー内に「コントローラー設定」を設ける。
- コントローラー設定では各操作部品を個別に移動・拡大縮小・透明度変更できる。
- 小さくしたボタンでも、選択状態なら画面上のピンチ操作でサイズ変更できる。
- 「デフォルトに戻す」を用意する。
- コントローラー設定の「完了」後はゲームへ直接戻らず、設定メニューへ戻る。

## ロックバスター

### 基本操作

- 自動ロックオンは使用しない。
- キャラクターが現在向いている方向へ射撃する。
- 移動中はスティックの移動方向を向く。
- 停止後は最後に移動していた方向を維持する。
- 射撃方向は360度。
- 移動しながら射撃・チャージ可能。
- Bタップ：通常弾1発。
- B連打：通常弾を連射。
- B長押し：チャージ。
- チャージ完了後にBを離す：チャージショット。

### 基礎性能（プロトタイプ）

- 通常弾・チャージ弾の射程：750。
- 通常バスター連射速度：約1.5発/秒。
- 通常弾の最短発射間隔：約0.67秒。
- チャージ時間などその他の数値は実機確認しながら調整する。

### 表現

- 通常弾は短いカプセル状のバスター弾を基本とする。
- チャージはキャラクターの射口にエネルギーが集まる表現とする。
- チャージ中は水色、チャージ完了時は赤へ変化させる。
- チャージショットは通常弾より大型で、短い残光を持たせる。
- 射口位置と弾の射出位置は同じ基準座標を使用する。
- 本格的なキャラクター描画を導入した際は、射口座標を再調整する可能性がある。

## ダッシュ

- Xタップで緊急回避ダッシュを行う。
- スティック入力中は現在の移動方向へダッシュする。
- 無入力時は最後に向いていた方向へダッシュする。
- ダッシュ距離：180（現時点の基準値）。
- ダッシュ時間：0.13秒（現時点の基準値）。
- クールタイム：0.65秒（現時点の基準値）。
- ダッシュ中は方向転換不可。
- ダッシュ開始から終了まで無敵とする。
- 将来の敵本体・敵弾の当たり判定では、ダッシュ中はすり抜け可能とする。
- クールタイム中はXボタンを暗く表示する。
- ダッシュ中はAによる新規チップ使用、Bによる新規射撃を不可とする。
- Bチャージ中にダッシュした場合、チャージ状態は維持する。
- ダッシュ終了直後から攻撃可能とする。

## バトルチップ

### 基本操作

- Aタップで現在セットされているバトルチップを使用する。
- カスタム画面で選択した順番を、そのまま戦闘中の使用順とする。
- 戦闘中の並び替えは不可。
- Aで先頭のチップを使用すると、自動的に次のチップへ進む。
- HUDには現在使用可能なチップと残りチップを表示する。
- チップごとに威力、射程、攻撃範囲、発動時間などを持てる設計とする。

### チップコード

- チップコードは現段階では実装しない。
- 将来必要になった場合に追加可能なデータ構造・選択ルールとする。

## カスタム画面

- 基本構造は原作方式をベースとする。
- 戦闘開始時にカスタム画面を表示してチップを選択する。
- カスタム画面を開いている間は戦闘を停止する。
- 初期のチップ表示枚数は5枚。
- 強化によって最大10枚まで表示可能にする想定。
- ソウルユニゾン相当の特殊枠を将来的に設けられる構造とする。ただし現段階では実装しない。
- カスタムゲージは戦闘中に蓄積し、通常状態では10秒でMAXとする。
- ゲージMAXでもカスタム画面は自動では開かない。
- ゲージMAX時のみ専用のCUSTOMボタンを表示または操作可能にする。
- CUSTOMボタンから任意のタイミングでカスタム画面を開く。
- YボタンはCUSTOMには使用せず、未定のまま確保する。

### 未使用チップとCUSTOM更新

原作方式を採用する。

- 現在の未使用チップが残った状態でCUSTOMを開ける。
- 新しいチップを1枚も選択せず確定した場合、現在の未使用チップを継続する。
- 新しいチップを1枚以上選択した場合、現在の未使用チップは破棄し、新しく選択したチップへ切り替える。
- 使用済み・破棄済みチップは、そのWave中は再登場しない。

## ウェーブ構成

### 基本構成

プロトタイプでは以下を基本ステージ構成とする。

1. Wave 1
2. Wave 2
3. Wave 3
4. BOSS

- ウェーブ数は固定値にせず、将来ステージごとに変更可能な設計とする。
- 短期ステージ、長期耐久ステージ、ボスのみのステージ等にも拡張可能とする。

### チップのリセット単位

- チップ管理上は各Waveを独立した単位として扱う。
- Wave開始時にフォルダを完全リセットする。
- 前Waveで使用済み・破棄済みとなったチップもすべて復活する。
- Wave 1 → Wave 2、Wave 2 → Wave 3、Wave 3 → BOSS の各移行時に同様にリセットする。
- BOSS開始時にもフォルダは完全な状態から開始する。

### 調整候補

実際にプレイした結果、360度戦闘による命中率低下などによってチップ消費が厳しすぎる場合は、以下への変更を検討する。

- Waveごとのフォルダ完全リセットは維持する。
- CUSTOM更新時の「未使用チップ破棄」を廃止する。
- 代わりに未使用チップをフォルダ／山札へ戻す方式へ変更する。

これは現時点では採用せず、プレイテスト後の調整候補として保持する。

## 将来実装候補：ナビカス系ステータス強化

現段階では実装しない。
基礎戦闘・素のプレイヤー性能を先に確定し、その後の成長・カスタマイズ要素として設計する。

### 強化候補

- バスター威力
- バスター連射速度
- チャージ速度
- 最大HP
- 移動速度
- ダッシュ性能／クールタイム
- カスタム画面のチップ表示枚数
- その他、戦闘基礎ステータス

### 設計上の注意

- 基礎性能の調整と強化補正を混在させない。
- まず強化なしの状態で戦闘バランスを成立させる。
- 強化上限、コスト、装備方式、組み合わせ制限などは後工程で決定する。
