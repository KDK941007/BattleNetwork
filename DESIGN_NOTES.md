# BattleNetwork 設計メモ

## 開発方針

- ロックマンエグゼ4の描画・戦闘感をベースにしつつ、指定フィールド内を自由移動する360度アクション戦闘とする。
- ウィルスやボスはウェーブ形式で出現する。
- 現段階では基礎戦闘・操作感を優先し、キャラクターやステージの本格ビジュアル制作は後工程とする。
- 通常は `main` を直接更新する。ブランチを分ける必要がある場合は別途指示する。
- 現在の実装状況と直近の作業対象は `DEVELOPMENT_STATUS.md` を最新情報として参照する。

## 現在の到達点（2026-08-23）

- フィールド移動、カメラ追従、ロックバスター、チャージショット、緊急回避ダッシュまで実装済み。
- CUSTOM画面は5×2の10枠を表示し、現時点では5枚を抽選して表示する。
- チップの選択／再タップによる選択解除、決定、チップコードによる複数選択制御を実装済み。
- 属性アイコンは属性ごとの共通画像素材を表示する方式で実装済み。
- キャノン、ソード、ワイドソード、ミニボム、リカバリーの5種類について正式イラストをCUSTOM画面へ反映済み。
- チップ画像はService Workerのオフラインキャッシュ対象へ追加済み。
- CUSTOM画面の画像表示・レイアウトは実機確認済みで問題なし。
- 新規チップ追加はイラスト制作工数が大きいため一旦後回しとし、既存5種類で基礎戦闘を作り込む。

## フィールド・カメラ

- 横画面を前提とする。
- フィールドは斜め上視点を考慮した正方形をベースとする。
- カメラは操作キャラクターを追従する。
- キャラクターはフィールド内を左スティックで360度自由移動する。

## コントローラー

- 左側：移動スティック。
- 右側：A / B / X / Y の4ボタン。
- A：バトルチップ使用。
- B：ロックバスター。
- X：緊急回避ダッシュ。
- Y：現時点では未定。将来機能用として確保する。
- 設定メニュー内に「コントローラー設定」を設ける。
- 各操作部品を個別に移動・拡大縮小・透明度変更できる。
- 「デフォルトに戻す」を用意する。
- コントローラー設定の「完了」後は設定メニューへ戻る。

## ロックバスター

- 自動ロックオンは使用しない。
- キャラクターが現在向いている方向へ360度射撃する。
- 移動中はスティック方向を向き、停止後は最後の方向を維持する。
- Bタップ：通常弾。B長押し：チャージ。離すとチャージショット。
- 通常弾・チャージ弾の射程：750。
- 通常バスター連射速度：約1.5発/秒（最短約0.67秒）。
- チャージは射口に水色→完了時赤のエネルギー表現。
- 射口位置と射出位置は同一基準座標とする。

## ダッシュ

- Xタップで緊急回避ダッシュ。
- 入力中は移動方向、無入力時は最後に向いていた方向へダッシュ。
- 距離：180、時間：0.13秒、クールタイム：0.65秒（現時点の基準値）。
- ダッシュ中は方向転換不可・無敵。
- ダッシュ中はA/Bの新規攻撃不可。
- Bチャージ中にダッシュした場合はチャージ状態を維持する。
- ダッシュ終了直後から攻撃可能。

## バトルチップ

### 基本操作

- Aで現在セットされているチップを使用する。
- CUSTOMで選択した順番をそのまま使用順とする。
- 戦闘中の並び替え不可。
- 使用後は自動的に次チップへ進む。
- HUDに現在チップと残りチップを表示する。
- チップコードは実装済み。CUSTOM画面に表示し、同一チップまたは同一コードを基準に複数選択できる。
- `*` コードはワイルドカードとして扱う。

### チップ画像・属性表示

- チップイラスト内にはチップ名、コード、属性、攻撃力等の情報を直接描かず、CUSTOM側UIで表示する。
- チップ画像の標準背景は `assets/chips/common/chip_background_standard.jpg` とする。
- 現在の正式画像は以下。
  - キャノン：`assets/chips/cannon.png`
  - ソード：`assets/chips/sword.png`
  - ワイドソード：`assets/chips/WideSwordpng.png`
  - ミニボム：`assets/chips/MiniBomb.png`
  - リカバリー10：`assets/chips/Recovery_10.png`
- 属性アイコンはチップごとに描画せず、属性別の共通画像を紐付けて表示する。
- 現在の5種類の画像はオフラインキャッシュ対象に含める。

### 次チップ攻撃範囲プレビュー

- Aで次に使用するチップの攻撃範囲を戦闘フィールドへ半透明表示する。
- キャラクターの向きへリアルタイム追従し、360度回転する。
- キャノン：前方への細長い射線。
- ソード：前方近距離の狭い帯状範囲。
- ワイドソード：ソードと同程度の射程で横幅の広い帯状範囲。
- ミニボム：投擲軌道と着弾地点の円形爆発範囲。
- リカバリー：攻撃範囲がないためプレビューなし。
- チップ消費後は即座に次チップのプレビューへ切り替える。

### 試作用5チップ

#### キャノン
- 向いている方向へ直進。追尾なし・非貫通。
- 射程はプロトタイプ仮値900。
- 発射時に一瞬硬直する。仮値0.25秒。

#### ソード
- 向いている方向の前方近距離・狭い帯状範囲を攻撃。
- 使用時に短い斬撃モーションと硬直を入れる。

#### ワイドソード
- ソードとほぼ同じ射程で横幅を広くする。
- 扇形ではなく短く横に広い帯状範囲。
- 使用時に短い斬撃モーションと硬直を入れる。

#### ミニボム
- 向いている方向の一定距離へ投擲し、着弾地点で円形範囲爆発。
- 自由な着弾地点指定は行わない。
- 投擲時に短い硬直を入れる。

#### リカバリー
- Aで使用するとHPを回復する。
- 攻撃範囲プレビューなし。
- 使用時に短い硬直と回復エフェクトを入れる。
- 正式イラストはリカバリー10として作成済み。
- 実際の回復量はHP設計後に確定する。

## カスタム画面

- 原作方式をベースとする。
- 戦闘開始時にCUSTOM画面を表示し、開いている間は戦闘停止。
- 画面上は5×2の10枠を常時確保する。
- 現時点では5枚を抽選して表示し、残り5枠は空枠とする。
- 将来的にCUSTOM表示枚数を最大10枚まで強化可能にする想定。
- 将来的にソウルユニゾン相当の特殊枠を追加可能にする。
- カスタムゲージは10秒でMAX。
- MAXでも自動では開かず、MAX時のみ専用CUSTOMボタンを有効化する。
- YはCUSTOMには使わない。
- チップ枠には正式チップ画像を表示し、画像枠外にチップ名、コード、属性等を表示する。
- チップは1回タップで選択し、選択中の同じチップを再タップするとキャンセルする。
- 「決定」と「ソウルユニゾン」のボタン枠を表示する。ソウルユニゾンは現時点では未実装。

### 未使用チップとCUSTOM更新

- 未使用チップが残った状態でもCUSTOMを開ける。
- 新しいチップを1枚も選択しなければ現在の未使用チップを継続。
- 新しいチップを1枚以上選択した場合、現在の未使用チップを破棄して新しいチップへ切り替える。
- 使用済み・破棄済みチップはそのWave中は再登場しない。

## ウェーブ構成

基本構成：Wave 1 → Wave 2 → Wave 3 → BOSS。

- ウェーブ数は固定せずステージごとに変更可能な設計とする。
- 各Waveをチップ管理上の独立単位とする。
- 各Wave開始時にフォルダを完全リセットし、使用済み・破棄済みチップもすべて復活する。
- BOSS開始時にも完全リセットする。

### 調整候補

プレイテストで360度戦闘によるチップ消費が厳しすぎる場合は、Waveごとの完全リセットを維持したまま、CUSTOM更新時の未使用チップを「破棄」ではなく「フォルダへ戻す」方式へ変更することを検討する。現時点では採用しない。

## 次フェーズ：バトル側の基礎作り込み

新規チップ追加は一旦停止し、既存の5種類だけで敵と実際に戦える状態を作る。

優先対象は以下。

1. HP・ダメージの基本設計。
2. バスター／既存5チップの攻撃判定と回復処理。
3. 雑魚敵1種類の実装。
4. 敵AI、敵攻撃、被弾処理。
5. 敵攻撃の予兆表示。
6. 攻撃・被弾・撃破時の最低限の演出。
7. 1体の敵との戦闘で操作感・チップ使用感を確認。
8. 基礎戦闘成立後にWave進行へ拡張。

このフェーズでは、新規チップや本格的な敵・ステージイラストの追加より、当たり判定、戦闘テンポ、操作感、視認性を優先する。

## 将来実装候補：ナビカス系ステータス強化

現段階では実装しない。基礎戦闘・素のプレイヤー性能を先に確定する。

強化候補：バスター威力、連射速度、チャージ速度、最大HP、移動速度、ダッシュ性能／クールタイム、CUSTOM表示枚数など。

- 基礎性能と強化補正を混在させない。
- まず強化なしで戦闘バランスを成立させる。
- 強化上限、コスト、装備方式、組み合わせ制限は後工程で決定する。
