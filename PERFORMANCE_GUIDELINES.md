# BattleNetwork パフォーマンス実装規約

最終更新: 2026-09-13

このドキュメントは、BattleNetwork の新機能・バトルチップ・敵・エフェクト・UIを追加／変更する際に必ず考慮するパフォーマンス規約を定義する。

## 最重要ルール

### 画面硬直ゼロを必須品質要件とする

バトル中の機能追加・変更によって、カスタムゲージ・プレイヤー操作・敵移動・敵攻撃・弾・カメラ・既存アニメーション等のリアルタイム処理を、意図せず1フレームでも止める実装を禁止する。

機能単体が正しく動いていても、攻撃使用時・命中時・状態遷移時・エフェクト開始／終了時などに画面全体の停止や引っ掛かりを発生させる場合は実装完了としない。

過去に一度発生・解決した種類のカクつきについては、ユーザーから毎回「画面を重くしないこと」と指定されなくても、実装時点で再発防止を必須で考慮する。

新しい種類の性能問題は発生後に計測して原因を特定するが、既知の問題パターンを繰り返さない。

## 実装時の必須確認

- 常駐する追加 `requestAnimationFrame` を安易に増やさない。既存ループやイベント駆動で処理できないかを先に検討する。
- 短周期の `setTimeout` / `setInterval` ポーリングを連続実行しない。イベント発生点から直接処理を接続する。
- 攻撃・命中・状態遷移・エフェクト発火のホットパスで、DOM/CSS変更後に `offsetWidth` / `offsetHeight` / `clientWidth` / `clientHeight` / `getBoundingClientRect()` 等を同期取得して強制レイアウトを発生させない。
- 要素サイズは初期化時に取得し、以後は `ResizeObserver` や `resize` イベント等で変更時だけ更新・キャッシュする。ゲームループや攻撃処理はキャッシュ値を参照する。
- 毎フレームのDOM生成・削除を避ける。弾・エフェクト等は可能な範囲で事前生成・再利用・プールする。
- バトル中に頻繁に切り替わる表示で `display:none/block` を繰り返さない。可能な場合は要素を常駐させ、`opacity` / `transform` / `animation-play-state` 等の合成処理を優先する。
- 一時エフェクトの開始時に「アニメーションを再起動するためだけ」の強制リフローを行わない。必要なら次フレームでclassを再付与する、Web Animations APIを使う、または常駐要素を再利用する。
- scene全体への `MutationObserver` を攻撃・弾・エフェクト検知のために常駐させない。発生元の処理から直接呼び出すか、既存の生成ポイントへ軽量なフックを接続する。
- 巨大なSVGやフィールド全面要素を一時エフェクトのためだけに生成しない。必要範囲に限定する。
- `filter`、大きな `box-shadow`、glow等の高コスト描画を高速弾・連射・多重エフェクトへ安易に使用しない。
- 同一フレーム／同一攻撃内で同じ命中判定・Range計算・DOM測定を重複実行しない。計算結果を再利用する。
- 多数の対象を処理する場合、同じ敵への重複判定・重複ダメージ処理を避ける。
- デバッグ表示やテスト機能自体が本体の動作を重くしないようにする。調整完了後の一時テストUIは本番経路から外す。

## 既知の事例からの再発防止

### Rangeプレビュー

過去にRange描画を操作イベントとゲームループの両方から実行して負荷が発生した。

- Range描画経路を重複させない。
- SVGの形状を毎フレーム再生成せず、固定形状＋transform等を優先する。
- Range表示用DOMはゲーム本体の巨大sceneから可能な範囲で分離する。
- sceneサイズはバトル中に同期測定せず、変更時だけキャッシュを更新する。

### バルカン

連射時の弾DOM・glow/filter等によってカスタムゲージ等へ影響するカクつきが発生した。

- 高頻度弾は軽量専用レイヤー／DOMプールを使用する。
- 高コストな影・glow・filterを連射弾へ使用しない。
- 弾の生成・削除回数を抑える。

### スプレッドガン

発射・誘爆時にカスタムゲージが一瞬止まる事象が発生した。

対策として以下を採用した。

- A押下後の短周期ポーリングを廃止し、初弾命中処理から誘爆を直接接続する。
- 初弾の同一命中計算を重複させず結果を再利用する。
- 誘爆用のフィールド全面SVG生成を避ける。
- 誘爆表示DOMを再利用する。
- 誘爆表示の非表示化で `display:none` を使わず `opacity` を利用する。
- 誘爆判定・表示処理は必要なタイミングだけ実行し、常駐計算を追加しない。

2026-08-31の実機確認では、上記対策後のスプレッドガンについて体感上の停止は問題ない状態まで改善した。

### フルシンクロ

2026-09-13、フルシンクロ状態で攻撃チップを使用してココロ値が 255 から 153 へ遷移する瞬間に、カスタムゲージ・敵移動・敵攻撃等が一瞬止まる事象が発生した。

原因になり得た実装は、リング追従処理で毎フレーム `offsetWidth` / `offsetHeight` を同期取得していたことと、状態遷移時にリングを `display` で出し入れしていたことだった。

対策として以下を採用した。

- リングサイズは初回取得後、`ResizeObserver` で変更時だけ更新する。
- 毎フレームはレイアウト測定を行わず、既存transformの追従だけにする。
- リング要素を常駐させ、表示切替は `opacity`、非アクティブ時は `animation-play-state: paused` を使用する。
- 状態遷移と同じフレームで不要なレイアウト・DOM生成／削除を発生させない。

2026-09-13の実機確認で、この対策後はフルシンクロ攻撃時の画面硬直が改善した。

## 性能問題が発生した場合

推測による軽量化を繰り返さない。

1. 問題が発生する操作とタイミングを限定する。
2. JavaScript処理時間とフレーム間隔を分けて計測する。
3. 発射・命中・エフェクト・DOM更新・レイアウト測定等を区間別に計測する。
4. 必要なら停止直前のイベント履歴を取得する。
5. 原因箇所を特定してから、その箇所だけを修正する。
6. ゲーム仕様・ダメージ・射程・タイミング等を性能対策のために勝手に変更しない。
7. 修正後はカスタムゲージ・敵移動・敵攻撃・弾・カメラ等、直接変更していないリアルタイム処理への影響も確認する。

## 実装完了の考え方

新しいリアルタイム機能は「その機能単体が動く」だけで完了としない。

既存のゲームループへ目立つ停止・カクつきを追加していないこと、攻撃・命中・状態遷移・エフェクト開始／終了時にも他のリアルタイム処理が継続することまでを実装品質として扱う。

コードレビュー時も、同期レイアウト取得、頻繁なDOM生成／削除、`display` 切替、scene全体監視、不要な常駐ループがホットパスへ入っていないかを確認する。

実機確認をしていない場合は、確認済みとは記載しない。
