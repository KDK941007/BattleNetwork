from pathlib import Path

path = Path('DEVELOPMENT_STATUS.md')
text = path.read_text(encoding='utf-8')

old = "v90で `BattleNetworkPlayerHealth.applyHealing()` を追加し、既存チップ「リカバリー10」を実HPへ接続した。回復量は既存マスタの `RECOVERY=10` をそのまま使用し、現在HPへ10加算する。回復後HPは最大HP100でクランプするため、例えば `80→90`、`95→100` となり100を超えない。HP変化は既存の購読API経由で左上HUDへ即時反映される。HP満タン時は新たな使用制限を追加せず、従来の `useChip()` 消費フローを維持したまま実回復量0となる。撃破後の蘇生は行わない。v90は被ダメージ後の10回復・上限100・HUD即時反映の実機確認待ち。"
new = "v90で `BattleNetworkPlayerHealth.applyHealing()` を追加し、既存チップ「リカバリー10」を実HPへ接続した。回復量は既存マスタの `RECOVERY=10` をそのまま使用し、現在HPへ10加算する。回復後HPは最大HP100でクランプするため、例えば `80→90`、`95→100` となり100を超えない。HP変化は既存の購読API経由で左上HUDへ即時反映される。HP満タン時は新たな使用制限を追加せず、従来の `useChip()` 消費フローを維持したまま実回復量0となる。撃破後の蘇生は行わない。実機確認で被ダメージ後の10回復、最大HP100での打ち止め、HUD即時反映に問題がないことを確認済み。"
if old not in text:
    raise SystemExit('v90 history text not found')
text = text.replace(old, new, 1)

old = "## 次フェーズ: v90 リカバリー10実機確認\n\nv82〜v85で左上HP表示とココロウィンドウ、v86で初期HP100、v87〜v88で被ダメージ経路とテスト敵攻撃、v89でキャノン非貫通まで実機確認済み。v90ではリカバリー10を実HPへ接続した。次は被ダメージ後にリカバリー10を使用し、10回復・最大HP100での打ち止め・HUD即時更新を実機確認する。確認後は被弾演出・無敵時間へ進む。\n\n優先対象は以下。\n\n1. HP90以下でリカバリー10を使用し、実HPが10回復することを確認する。\n2. HP95など上限付近では100までだけ回復し、最大HPを超えないことを確認する。\n3. 回復時に左上HUDへ即時反映されることを確認する。\n4. v90確認後、被弾演出・無敵時間を設計・実装する。\n5. その後、ノックバック・撃破処理へ拡張する。\n6. 敵AI・Wave進行へ段階的に進める。"
new = "## 次フェーズ: 被弾演出・無敵時間\n\nv82〜v85で左上HP表示とココロウィンドウ、v86で初期HP100、v87〜v88で被ダメージ経路とテスト敵攻撃、v89でキャノン非貫通、v90でリカバリー10の実HP回復まで実機確認済み。次は被弾したことを視覚的に分かるようにする被弾演出と、連続接触でHPが瞬時に削れ続けないための無敵時間を設計・実装する。具体的な演出方式・無敵時間の秒数は既存仕様を確認したうえで確定し、推測では決めない。\n\n優先対象は以下。\n\n1. 被弾演出の表示方式を確定する。\n2. 被弾後の無敵時間の仕様と具体値を確定する。\n3. v87の共通被ダメージ経路へ被弾状態を接続する。\n4. 無敵時間中の重複ダメージを防止し、HUD更新との整合を確認する。\n5. その後、ノックバック・撃破処理へ拡張する。\n6. 敵AI・Wave進行へ段階的に進める。"
if old not in text:
    raise SystemExit('next phase text not found')
text = text.replace(old, new, 1)

path.write_text(text, encoding='utf-8')
