from pathlib import Path

path = Path('GAME_DESIGN.md')
text = path.read_text(encoding='utf-8')
old = '''### 敵共通: プレイヤー知覚距離

- 敵は常にフィールド全域のプレイヤー位置を追跡するのではなく、敵タイプごとに個別のプレイヤー知覚距離を持つ。
- 知覚距離はマス数を設計単位とし、敵タイプごとに異なる値を設定可能とする。
- プレイヤーが知覚範囲外にいる間は、原則としてその敵固有の待機・巡回等を行い、プレイヤーを直接狙う追跡・攻撃行動へ移らない。
- プレイヤーを一度知覚した後も、フィールド全域を無期限に追跡し続ける仕様にはしない。
- 知覚開始距離と知覚解除距離を同一にするか、境界付近での状態切替を防ぐため解除距離を別に持つかは後続設計で確定する。
- ボス、固定砲台、特殊ギミック等で通常の知覚距離を使用しない場合は、その敵固有仕様として明示する。
'''
new = '''### 敵共通: プレイヤー知覚距離

- 敵は常にフィールド全域のプレイヤー位置を追跡するのではなく、敵タイプごとに個別のプレイヤー知覚距離を持つ。
- 知覚距離はマス数を設計単位とし、敵タイプごとに異なる値を設定可能とする。
- プレイヤーが知覚範囲外にいる間は、原則としてその敵固有の待機・巡回等を行い、プレイヤーを直接狙う追跡・攻撃行動へ移らない。
- プレイヤーを一度知覚した後も、フィールド全域を無期限に追跡し続ける仕様を全敵共通にはしない。
- 敵タイプごとに `追跡限界距離を使用する / 使用しない` を設定可能とする。追跡限界距離を使用する敵は、初期位置または敵固有の縄張り基準点から設定距離を超えた時点で追跡を解除し、待機・巡回・帰還等の敵固有状態へ移行する。
- 追跡限界距離を使用しない敵は、距離による強制的な追跡解除を行わない。ただし、追跡を永久継続することを意味せず、追跡終了条件はその敵固有AIで別途定義できる。
- 追跡限界距離そのものもマス数を設計単位とし、ONの場合のみ敵タイプごとに個別設定する。
- 知覚開始距離と知覚解除距離を同一にするか、境界付近での状態切替を防ぐため解除距離を別に持つかは後続設計で確定する。
- ボス、固定砲台、特殊ギミック等で通常の知覚距離を使用しない場合は、その敵固有仕様として明示する。
'''
if old not in text:
    raise SystemExit('target block not found')
path.write_text(text.replace(old, new, 1), encoding='utf-8')
