# BattleNetwork チップイラスト制作ガイドライン

CUSTOM画面用バトルチップ画像の共通制作手順・編集ルール

**対象リポジトリ:** KDK941007/BattleNetwork  
**対象ブランチ:** main  
**対象素材:** CUSTOM画面で使用するバトルチップ画像

## 1. 目的

本書は、BattleNetwork風ゲームのCUSTOM画面で使用するチップイラストを、既存素材との統一感を維持しながら制作・修正するための共通ルールを定める。

最も重要な原則は、**確定済み画像をロックされた仕様として扱うこと**である。

ユーザーが「絵はそのまま」「ここだけ修正」と指定した場合は、指定された箇所以外を変更しない。

「似た画像を新しく作る」のではなく、**元画像への限定的な差分編集**として処理する。

## 2. 素材参照先と共通背景

既存チップイラストを制作・修正する際は、以下を基準とする。

### 既存チップイラストの参照先

`assets/chips/`

制作対象に最も近い既存チップ画像をベース画像として選択する。

### 共通背景

`assets/chips/common/chip_background_standard.jpg`

全チップ画像でこの背景を共通使用する。

チップごとに別の背景を生成してはいけない。

Windows上の表記では以下となる。

`\assets\chips\common\chip_background_standard.jpg`

## 3. チップ画像の共通仕様

画像内には以下を入れない。

- チップ名
- チップコード
- 属性名
- 攻撃力
- UIフレーム

これらの情報はゲーム側UIで別途表示する。

## 4. ベース画像の選び方

1. `assets/chips/` 配下から、制作対象に最も構造・系統が近い既存画像を選ぶ。
2. ベース画像の構図・画風・質感・配置は原則として維持する。
3. 本家作品などの参考画像は、形状差・配色差・装飾差を確認するための資料として使用する。
4. 参考画像そのものを編集対象にしたり、参考画像の構図や背景へ寄せたりしない。

たとえばWide Swordを制作する場合は、`assets/chips/sword.png` を編集対象とする。

本家Wide Sword画像は、刃先形状や装飾差を確認するためだけに使用する。

## 5. 最重要編集ルール

ユーザーが確定した画像を基準とし、**変更許可が明示された箇所だけを編集する。**

### 5.1 変更してよいもの

ユーザーから明示的に指定された以下の変更のみ実施する。

- 形状変更
- 色変更
- 装飾の追加・削除
- 位置調整
- 向き調整
- 大きさ調整

### 5.2 指示がない限り変更してはいけないもの

- 構図
- 画風
- 色
- 背景
- 武器やオブジェクト本体
- パーツ形状
- エフェクト
- 向き
- 大きさ
- 質感
- カメラ角度
- 光沢
- 陰影
- 線のタッチ
- 全体配置

「変更した方が良さそう」に見えても、指示されていなければ変更しない。

改善案がある場合は画像へ勝手に反映せず、必要に応じて別途提案する。

## 6. 制作フロー

1. **制作対象を特定する**  
   チップ名、想定ファイル名、ベースにする既存チップを確認する。

2. **ベース画像を固定する**  
   `assets/chips/` 配下から編集対象となる既存画像を1枚決める。

3. **変更点を限定する**  
   「変更してよい箇所」を明確にする。

4. **維持項目を明示する**  
   背景・構図・画風・向き・サイズなど、変更禁止項目を明確にする。

5. **局所編集として生成する**  
   全体再生成ではなく、既存画像への差分修正として処理する。

6. **ベース画像と比較する**  
   指定箇所以外に差分が発生していないか確認する。

7. **問題箇所だけを再修正する**  
   問題があっても画像全体を作り直さず、該当箇所だけを修正する。

8. **ユーザーのOK後に正式採用する**  
   確定前の画像を正式素材として登録しない。

## 7. 画像編集AIへの基本指示テンプレート

編集対象は `assets/chips/{ベース画像ファイル名}`。

この画像を新規生成し直してはいけない。  
既存画像を可能な限りそのまま保持する。

変更するのは次の箇所だけ。

1. `{変更点1}`
2. `{変更点2}`

それ以外は変更しない。

特に以下は完全に維持する。

- 共通背景
- 構図
- カメラアングル
- オブジェクトの向き
- オブジェクトの位置
- 基本形状
- 画風
- 陰影
- 光沢
- 描画品質
- 全体サイズ

背景は `assets/chips/common/chip_background_standard.jpg` と同じものを使用する。

「似た新しい画像」を作るのではなく、**元画像への局所編集**として処理する。

## 8. 生成後の確認チェックリスト

- [ ] ベース画像と並べて違和感がないか
- [ ] 共通背景が `assets/chips/common/chip_background_standard.jpg` と同系統か
- [ ] 指定していない部分の形状が変わっていないか
- [ ] 構図・カメラ角度・向きが変わっていないか
- [ ] 色・質感・陰影・光沢が勝手に変更されていないか
- [ ] 指示した変更点だけが反映されているか
- [ ] 余計な装飾やエフェクトが追加されていないか
- [ ] 既存チップ素材と品質感が揃っているか
- [ ] ユーザーが確定した要素を壊していないか

## 9. 禁止例

### NG1

既存画像を参考にしただけの「似た別イラスト」を新規生成する。

### NG2

局所修正のつもりで、構図・背景・画風まで描き直す。

### NG3

共通背景を別のサイバー背景、灰色背景、タイル背景などへ変更する。

### NG4

ユーザーが指示していない色・エフェクト・装飾を追加する。

### NG5

一部パーツを修正するついでに、本体形状や接続構造まで変更する。

### NG6

修正結果に問題がある際、該当箇所だけでなく画像全体を再生成する。

## 10. 局所編集の具体例：Wide Sword

Wide Swordは、既存Swordを基準にした局所編集の代表例として扱う。

### 編集対象

`assets/chips/sword.png`

### 参考画像

本家Wide Sword画像。  
形状差を確認するためだけに使用する。

### 変更1

刃先を「イカの頭」のように左右へ張り出した幅広形状へ変更する。

### 変更2

青い腕ユニットに黄色／金色の装飾パーツを追加する。

### 維持するもの

背景、構図、カメラ角度、向き、腕ユニットの基本形状、刃の発光色、画風、陰影、品質、サイズ。

### 禁止するもの

別のSFソード化、手持ち剣化、柄の追加、背景変更、余計なエフェクト追加、ブレード全体の再設計。

## 11. 正式採用・配置ルール

ユーザーが「OK」「これで確定」と判断するまでは正式素材として扱わない。

確定後は、対象チップ名に対応したファイル名で `assets/chips/` 配下へ配置する。

既存の確定済み素材は勝手に上書き・再生成しない。

チップ画像が揃った後、CUSTOM画面からの参照実装およびService Workerのオフラインキャッシュ対象追加を行う。

## 12. 運用時の短縮ルール

**以下の3点を必ず守る。**

1. 既存チップは `assets/chips/` 配下を参照する。
2. 背景は `assets/chips/common/chip_background_standard.jpg` を共通使用する。
3. 「絵はそのまま」「ここだけ修正」は、指定箇所以外を変更しない局所編集として扱う。
