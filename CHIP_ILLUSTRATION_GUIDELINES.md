# BattleNetwork チップイラスト制作ガイドライン

CUSTOM画面用バトルチップ画像の共通制作手順・編集ルール

**対象リポジトリ:** KDK941007/BattleNetwork  
**対象ブランチ:** main  
**対象素材:** CUSTOM画面で使用するバトルチップ画像

## 1. 目的

本書は、BattleNetwork風ゲームのCUSTOM画面で使用するチップイラストを、原作デザインを参考にしつつ、既存素材と同等の雰囲気・画風・クオリティを維持して制作・修正するための共通ルールを定める。

新規チップイラストを制作する場合は、**原作の該当チップイラストをデザイン上の参考資料として使用し、それに似た新しいイラストを制作する。**

`assets/chips/` 配下の既存画像は、作成するイラストの雰囲気・画風・質感・描画品質・全体クオリティを揃えるための参考資料として使用する。

**`assets/chips/` 配下の画像をベース画像として直接編集し、新しいチップへ作り替えてはいけない。**

一方、ユーザーがすでに制作結果を確認し、「絵はそのまま」「ここだけ修正」などと指定した場合は、その時点の画像を確定済みの編集対象として扱い、指定された箇所以外を変更しない。

## 2. 参照素材と共通背景

チップイラストを制作・修正する際は、以下を基準とする。

### 原作イラストの参照

新規チップイラストを制作する場合は、原作の該当チップイラストを主なデザイン参考資料とする。

原作イラストから、以下の要素を確認する。

- モチーフ
- 基本形状
- パーツ構成
- 配色
- 装飾
- 向き
- 特徴的なエフェクト

原作画像そのものを本プロジェクト用画像として使用するのではなく、原作の特徴を参考にした新しいイラストを制作する。

### クオリティ参照先

`assets/chips/`

このフォルダ配下の既存チップ画像は、以下を揃えるための品質参考資料として使用する。

- 画風
- 質感
- 陰影
- 光沢
- 線や輪郭の表現
- オブジェクトの見せ方
- 描画密度
- 全体的な完成度

**あくまで雰囲気やクオリティを同等にするための参照であり、既存画像をベースにして別チップへ編集するものではない。**

### 共通背景

`assets/chips/common/chip_background_standard.jpg`

全チップ画像でこの背景を共通使用する。

チップごとに別の背景を生成してはいけない。

Windows上の表記では以下となる。

`\assets\chips\common\chip_background_standard.jpg`

## 3. チップ画像の共通仕様

画像内には以下を入れない。

- チップ名
- チップコード
- 属性名
- 攻撃力
- UIフレーム

これらの情報はゲーム側UIで別途表示する。

## 4. 参考画像の使い分け

新規制作時は、原作画像と `assets/chips/` 配下の画像を明確に役割分担して使用する。

### 原作画像

**何を描くかを決めるためのデザイン参考資料。**

原作の該当チップイラストを見て、形状・配色・装飾・向き・モチーフなど、そのチップ固有の特徴を把握する。

### `assets/chips/` 配下の既存画像

**どの程度の雰囲気・画風・クオリティで描くかを決めるための品質参考資料。**

既存画像の構図やオブジェクトを流用したり、既存画像そのものを編集元として別のチップへ変形させたりしない。

### 共通背景

背景については新規生成せず、`assets/chips/common/chip_background_standard.jpg` を共通使用する。

## 5. 確定済み画像の修正ルール

本節は、新規制作ではなく、**一度生成した画像に対してユーザーから修正指示が出た場合**に適用する。

ユーザーが確定または編集元として指定した画像を基準とし、**変更許可が明示された箇所だけを編集する。**

### 5.1 変更してよいもの

ユーザーから明示的に指定された以下の変更のみ実施する。

- 形状変更
- 色変更
- 装飾の追加・削除
- 位置調整
- 向き調整
- 大きさ調整

### 5.2 指示がない限り変更してはいけないもの

- 構図
- 画風
- 色
- 背景
- 武器やオブジェクト本体
- パーツ形状
- エフェクト
- 向き
- 大きさ
- 質感
- カメラ角度
- 光沢
- 陰影
- 線のタッチ
- 全体配置

「変更した方が良さそう」に見えても、指示されていなければ変更しない。

改善案がある場合は画像へ勝手に反映せず、必要に応じて別途提案する。

## 6. 新規チップイラストの制作フロー

1. **制作対象を特定する**  
   チップ名と想定ファイル名を確認する。

2. **原作イラストを確認する**  
   該当チップの原作イラストから、モチーフ・形状・配色・装飾・向きなどの特徴を把握する。

3. **品質参考画像を確認する**  
   `assets/chips/` 配下の既存画像を確認し、画風・質感・陰影・光沢・描画密度・完成度の基準を把握する。

4. **共通背景を固定する**  
   `assets/chips/common/chip_background_standard.jpg` を使用する。

5. **新規イラストとして生成する**  
   原作の特徴を参考にしながら、既存チップ素材と同等の雰囲気・クオリティを持つ新しいイラストを制作する。

6. **既存チップ素材と品質を比較する**  
   `assets/chips/` 配下の画像と並べ、画風・質感・描画品質に大きな差がないか確認する。

7. **原作の特徴を確認する**  
   原作チップ固有の形状・配色・装飾などが適切に反映されているか確認する。

8. **ユーザーのOK後に正式採用する**  
   確定前の画像を正式素材として登録しない。

## 7. 画像生成AIへの基本指示テンプレート

### 7.1 新規制作時

原作の `{チップ名}` のイラストをデザイン参考資料として使用する。

原作から以下の特徴を参考にし、それに似た新しいチップイラストを制作する。

- 基本形状
- パーツ構成
- 配色
- 装飾
- 向き
- 特徴的なエフェクト

描画の雰囲気・画風・質感・陰影・光沢・完成度は、`assets/chips/` 配下の既存チップ画像を参考にし、同等のクオリティに揃える。

ただし、`assets/chips/` 配下の画像を編集元またはベース画像として使用してはいけない。

背景は `assets/chips/common/chip_background_standard.jpg` を共通使用する。

画像内にチップ名、チップコード、属性名、攻撃力、UIフレームを入れない。

### 7.2 確定済み画像の修正時

編集対象は、ユーザーが編集元として指定した現在の画像とする。

この画像を新規生成し直してはいけない。  
既存画像を可能な限りそのまま保持する。

変更するのは次の箇所だけ。

1. `{変更点1}`
2. `{変更点2}`

それ以外は変更しない。

特に以下は完全に維持する。

- 共通背景
- 構図
- カメラアングル
- オブジェクトの向き
- オブジェクトの位置
- 指示対象外の基本形状
- 画風
- 陰影
- 光沢
- 描画品質
- 全体サイズ

修正は、**現在の画像への局所編集**として処理する。

## 8. 生成後の確認チェックリスト

### 新規制作時

- [ ] 原作チップの特徴が反映されているか
- [ ] 原作とは別の、本プロジェクト用のイラストとして制作されているか
- [ ] `assets/chips/` 配下の画像をベースに直接編集していないか
- [ ] 既存チップ素材と画風・質感・描画品質が揃っているか
- [ ] 共通背景が `assets/chips/common/chip_background_standard.jpg` になっているか
- [ ] チップ名・コード・属性名・攻撃力・UIフレームが入っていないか
- [ ] 不要な装飾やエフェクトが追加されていないか

### 確定済み画像の修正時

- [ ] 編集元として指定された画像を基準にしているか
- [ ] 指定していない部分の形状が変わっていないか
- [ ] 構図・カメラ角度・向きが変わっていないか
- [ ] 色・質感・陰影・光沢が勝手に変更されていないか
- [ ] 指示した変更点だけが反映されているか
- [ ] 余計な装飾やエフェクトが追加されていないか
- [ ] ユーザーが確定した要素を壊していないか

## 9. 禁止例

### NG1

新規チップ制作時に、`assets/chips/` 配下の既存画像をベース画像として別チップへ編集・変形する。

### NG2

原作イラストではなく、既存チップ画像の形状や構図を流用して制作対象のデザインを決める。

### NG3

共通背景を別のサイバー背景、灰色背景、タイル背景などへ変更する。

### NG4

原作にない不要な色・エフェクト・装飾を勝手に追加する。

### NG5

確定済み画像の一部を修正するついでに、指示されていない本体形状や接続構造まで変更する。

### NG6

確定済み画像の修正結果に問題がある際、該当箇所だけでなく画像全体を再生成する。

## 10. 新規制作の具体例：Wide Sword

Wide Swordを新規制作する場合、原作Wide Swordのイラストをデザイン参考資料とする。

### デザイン参考

原作Wide Sword画像。  
刃の形状、配色、装飾、向きなど、そのチップ固有の特徴を確認するために使用する。

### 品質参考

`assets/chips/` 配下の既存チップ画像。  
画風、質感、陰影、光沢、描画密度、完成度を揃えるために使用する。

既存Swordなどをベース画像として編集し、Wide Swordへ作り替えてはいけない。

### 背景

`assets/chips/common/chip_background_standard.jpg` を使用する。

### 制作方針

原作Wide Swordの特徴を参考にしながら、新しいイラストとして制作する。

既存チップ画像とは、デザインそのものではなく、雰囲気・画風・クオリティを揃える。

## 11. 正式採用・配置ルール

ユーザーが「OK」「これで確定」と判断するまでは正式素材として扱わない。

確定後は、対象チップ名に対応したファイル名で `assets/chips/` 配下へ配置する。

既存の確定済み素材は勝手に上書き・再生成しない。

チップ画像が揃った後、CUSTOM画面からの参照実装およびService Workerのオフラインキャッシュ対象追加を行う。

## 12. 運用時の短縮ルール

**以下の4点を必ず守る。**

1. 新規チップは、原作の該当チップイラストをデザイン参考として似た新しいイラストを制作する。
2. `assets/chips/` 配下の画像は、雰囲気・画風・質感・クオリティの参照にのみ使用し、ベース画像として編集しない。
3. 背景は `assets/chips/common/chip_background_standard.jpg` を共通使用する。
4. 「絵はそのまま」「ここだけ修正」は、ユーザーが指定した現在の画像を対象に、指定箇所以外を変更しない局所編集として扱う。
