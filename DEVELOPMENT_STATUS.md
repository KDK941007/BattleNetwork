# BattleNetwork 開発状況

最終更新: 2026-08-26

このドキュメントは、現在の実装状況と直近の開発方針を管理する。
ゲーム全体の構想は `GAME_DESIGN.md`、詳細な実装・設計メモは `DESIGN_NOTES.md`、チップマスタとセーブデータの正式設計は `MASTER_DATA_DESIGN.md` を参照する。
フィールドの論理マス・地形・自由移動と攻撃範囲の正式設計は `FIELD_COMBAT_DESIGN.md` を参照する。

## 現在のフェーズ

基礎操作とCUSTOM画面のプロトタイプが成立し、初期5種類のバトルチップについてCUSTOM画面で正式イラストを表示できる状態まで完了した。

2026-08-24に、チップ増加・フォルダ編集・レギュラーチップ・ガチャ/報酬・セーブデータを見据え、チップ固定情報のJSマスタ化とIndexedDB v1の基盤を追加した。
同日にチップ詳細上半分もマスタ参照へ接続し、基本情報・複数主要値・複数属性/系統を固定枠内で表示できる構成へ更新した。

2026-08-24に、フィールドを単なる装飾模様ではなく20×20の論理マスとして定義し、360度自由移動・自由方向攻撃を維持したまま、射程や幅をマス数で設計する方針を確定した。
2026-08-25に、Rangeを純粋な形状、Behaviorを効果の届け方として分離し、`LINE / RECT / CIRCLE / SECTOR / RING / SELF` の基本Range Typeと各Parameterを確定した。

同日、`js/field/field-grid.js` を追加し、20×20論理グリッドの内部基盤を実装した。
さらに `js/field/field-grid-renderer.js` と `css/field-grid.css` を追加し、従来の装飾用繰り返し模様を廃止して、20×20の論理マス境界から生成する床グリッド表示へ切り替えた。
床表示は実機確認で良好と判断し、現行サイズ `TILE_SIZE=180` を継続する。将来の調整余地は残す。

同日、プレイヤーの足元ワールド座標 `s.x / s.y` を論理グリッドへ接続し、移動中の所属マス `row / col` と現在地形をゲームループ内で追跡する処理を実装した。

さらに、キャノン・ソード・ワイドソードのRangeマスタをマス単位へ移行し、`js/combat/range-geometry.js` で360度自由方向のRange形状をworld座標上に生成する共通基盤を追加した。
`js/combat/range-preview-renderer.js` と `css/range-preview.css` により、同じRange形状を現在の斜め投影へ変換してプレビュー表示する方式へ切り替えた。
初回プレビューは描画中の操作負荷が大きかったが、v43でSceneサイズ・投影係数のキャッシュ等を実施し、実機で改善済み。

ミニボムはv44で `0.5 / 0.75 / 1.0マス` の3候補を比較し、360度自由移動環境での当てやすさを考慮して `radius_tiles=0.75` を正式採用した。
v46で比較用Y操作と表示を削除し、投擲距離を原作イメージに合わせて `3マス` に変更した。投擲予告線は直線から放物線へ変更した。
同時にCIRCLEプレビューを多頂点ポリゴンではなく投影後のSVG楕円で描画し、カスタムゲージ更新時の二重Range描画も解消した。実機確認では半径0.75マス、3マス投擲、放物線の見た目は問題なし。ただしミニボムおよびキャノンのRange表示中は、移動・旋回時の負荷が改善しきらず、ちょくちょく重くなる状態を確認した。
v47で `joyMove()` 内の `renderPreview()` を削除し、スティック操作イベントとゲームループの双方からRange描画していた重複を解消した。Range描画はゲームループ側の1回/フレームへ統一した。
実機再確認ではミニボムは若干改善したが断続的な重さが残り、キャノンは依然として重さが目立つことを確認した。またCUSTOMゲージMAX後、初回タップではCUSTOM画面が開かず2回目以降で開くように見える事象を確認した。
v48で毎フレーム実行していた `battle.getBoundingClientRect()` を廃止し、battleサイズは初期化時とresize時だけ取得するよう変更した。スティックもpointermoveごとの `joy.getBoundingClientRect()` を廃止し、pointerdown時に取得した矩形をドラッグ中に再利用する。所属マスの `data-*` 属性更新もrow/col/terrainが変化した時だけ実施する。CUSTOMゲージは共通ハンドラを `pointerdown` と `click` の双方へ接続し、二重発火を防ぎつつ初回タップの取りこぼしを補完した。
v48実機確認でもRange表示中の移動・旋回負荷は改善せず、CUSTOMゲージも1回目で開く場合はあるものの2回目以降で開くことの方が多い状態を確認した。
v49ではRangeプレビューのSVG座標更新を最大約30fpsへ制限し、ゲーム本体の60fps相当ループやA押下時の正式Range Geometryとは分離した。キャノンのpolygonとミニボムのellipse／放物線pathのDOM更新頻度を抑えた。CUSTOMゲージは `div role=button` からネイティブの `button` 要素へ変更した。
v49実機確認でもRange表示中の移動・旋回負荷とCUSTOMゲージの初回タップ問題は改善しなかった。さらにCUSTOMゲージ内部を `span` へ変更した影響で、ゲージが溜まる幅アニメーションが表示されない退行を確認した。
v50では30fps制限を撤回し、LINE / RECTはローカル座標のpolygonを固定したままSVG `matrix()` transformで位置・向きを更新する方式へ変更した。CIRCLEは半径形状を固定し、中心位置のみtranslateで更新する。ミニボム放物線もローカルpathとtranslateを分離し、移動時のpath再生成を抑える。CUSTOMゲージはFillをblock表示へ戻して幅アニメーションを復旧し、見た目を変えずに疑似要素でタップ領域だけを上下左右へ拡張した。
v50実機確認では、CUSTOMゲージMAX後の初回タップと蓄積アニメーションは改善済み。Range表示中の移動・旋回も多少改善したが、まだところどころで重くなる状態が残る。
v51ではRange SVGを床・プレイヤー・弾と同じ巨大 `scene` から分離し、`battle` 直下の専用 `combatPreviewLayer` へ移した。専用レイヤーは `scene.style.transform` のみ同期し、Range描画とゲーム本体DOMツリーの再描画を分離する。Range Geometry、攻撃判定値、見た目のRange値は変更しない。
v51実機確認では、稀に重くなることは残るものの大幅に改善され、現時点では実用上許容としてこの負荷対応を完了扱いとする。将来、戦闘要素追加後に再び負荷が顕在化した場合は再調査する。

2026-08-25に敵HitBox基盤へ着手し、`js/combat/enemy-foundation.js` に簡易テスト敵、world座標、矩形HitBox、HitBox境界取得・点判定の最小基盤を追加した。
敵は表示とHitBoxを分離し、`visual.width / visual.height / visual.offsetX / visual.offsetY` と `hitBox.width / hitBox.height / hitBox.offsetX / hitBox.offsetY` を独立して持つ。具体値は本番キャラ素材に合わせて個別調整し、表示サイズからHitBoxを自動決定しない。

v53で `BattleNetworkRangeGeometry.intersectsBounds()` を追加し、LINE / RECTは斜め向きRange矩形と敵の軸平行矩形HitBoxをSATで交差判定、CIRCLEは円と矩形の最短距離で交差判定できるようにした。
`BattleNetworkEnemy.getHitEnemies(shape)` を追加し、正式Range Geometryと敵HitBoxの幾何交差を共通取得できるようにした。キャノン / ソード / ワイドソード / ミニボムで実機確認し、想定どおり命中可視化されることを確認済み。
v54でBehaviorと命中タイミングを接続し、キャノンは弾の到達時、ソード / ワイドソードは発動時、ミニボムは爆発遅延後に敵HitBoxへ命中可視化する構成へ変更。実機確認済み。
v55で通常ロックバスター / チャージショットも飛翔中のworld座標と敵HitBoxを接続し、命中時に発光・弾消滅する構成へ変更。実機確認済み。
v56でキャノン / 通常バスター / チャージショットへ床面上の正式world座標を示す影を追加した。
v57で視認性確認用として通常バスターを30×15px、チャージショットを52×26px、キャノンを44×22pxへ拡大し、影も拡大。実機で良好と確認済み。
v58でキャノンをさらに88×44pxへ倍化し、影を76×26pxへ拡大。同時に見た目と正式判定を揃えるため、キャノンRangeを `LINE(5×0.5)`、幅90 world unitsへ変更した。射程5マス、弾速、攻撃力は変更していない。実機確認では方向性は良好で、さらに約1.5倍大きくする方針を採用した。
v59でキャノン表示を132×66pxへ拡大し、影を114×39pxへ変更。正式Rangeを `LINE(5×0.75)`、幅135 world unitsへ拡大した。射程5マス、弾速、攻撃力は変更していない。またRange表示が実攻撃を隠しやすかったため、Range SVG全体をopacity 0.45、ミニボム投擲軌道をopacity 0.55へ透明化した。実機確認ではサイズ・Range・透明度は改善を確認したが、大型化したキャノン弾が床面を這うように見える表示上の問題を確認した。
v60で弾サイズに依存しない表示補正へ変更し、キャノン / 通常バスター / チャージショットの見た目中心を正式world座標へ合わせた。弾の下端は床面から約20px浮くよう実サイズから自動補正し、床影も弾サイズではなく正式な床面world座標を基準に配置する。命中判定・弾速・Range値は変更していない。実機確認で、キャノンが床を這わず影との高さ感も自然になったことを確認済み。
v61でプレイヤー表示を44×56pxから54×68px、テスト敵表示を48×58pxから58×70pxへ一回り拡大した。敵HitBoxは `0.55マス` 四方から `0.66マス` 四方（118.8 world units）へ拡大。プレイヤーにも表示と分離した `0.66マス` 四方のworld矩形HitBox基盤を追加し、`BattleNetworkPlayer` から位置・HitBox境界・点包含判定を参照できるようにした。v61は実機確認前にv62へ拡大方針を変更したため、v61単独では確定扱いにしない。
v62でv61値からさらに2倍へ拡大し、プレイヤー表示を108×136px、テスト敵表示を116×140pxへ変更した。プレイヤー・敵HitBoxも `0.66マス` 四方から `1.32マス` 四方（237.6 world units）へ倍化した。表示とHitBoxの分離、足元world座標、既存Range Geometry・弾速・攻撃Rangeは変更していない。表示サイズは実機確認で良好。HitBox倍化後の命中感は引き続き確認対象とする。
v63でカメラをもう少し引いた見え方を比較するため、`js/ui/camera-zoom-comparison.js` を一時追加した。候補は `0.58 / 0.54 / 0.50` の3段階でYボタン切替としたが、固定倍率を選択する方式自体を採用しない方針へ変更したため、v64で撤去した。
v64で `js/ui/camera-zoom-settings.js` を追加し、コントローラー配置編集画面の上部バーへカメラ倍率スライダーを常設した。倍率は `0.50〜0.62` を `0.01` 刻みで変更でき、初期値は `0.58`。変更は即時にフィールドへ反映し、`localStorage` の `battleNetworkCameraZoomV1` へ保存する。「デフォルトに戻す」で倍率も0.58へ戻る。v63の一時比較スクリプト・右上比較表示・Yボタン切替は削除した。
v65で配置編集バーをCUSTOMゲージと同じ `min(560px,68vw)` 幅へ広げ、`top:4px` に移動してCUSTOMゲージと重なる位置へ変更。カメラ倍率スライダー・倍率値・デフォルト・完了ボタンが1列で崩れにくいレイアウトへ整理した。カメラ倍率下限は `0.50` から `0.40` へ拡張し、現行範囲を `0.40〜0.62`、0.01刻みとした。またYボタンの正式用途を方向固定トグルに決定し実装。1回タップでその時点のキャラクター向き・攻撃方向を固定し、固定中も移動方向は自由、再タップで固定解除とする。固定中はYボタンを押下状態の色へ変更する。v65は実機確認待ち。
v66でカメラ倍率下限を `0.20` まで拡張し、現行設定範囲を `0.20〜0.62`、0.01刻みとした。またワイドソード画像の正式ファイル名変更 `WideSwordpng.png` → `WideSword.png` に合わせ、`chip-master.js` の画像参照とService Workerのキャッシュ対象を `assets/chips/WideSword.png` へ更新した。カメラ下限0.20とワイドソード画像再表示は実機確認済み。
v67でチップ詳細画面の攻撃範囲イメージ図を、旧来の雰囲気図から論理マス基準の7×5縮小グリッドへ変更した。図は `BattleNetworkMaster.getRangeParams()` と `getBehaviorParams()` を参照し、キャノン `5×0.75`、ソード `1×1`、ワイドソード `1×3`、ミニボム `3マス先 / 半径0.75`、リカバリー10 `SELF` をマスタ値から描画する。プレイヤーはマスへスナップする仕様ではないため、グリッド上の任意位置を起点としてRange長・幅・半径をマス縮尺で重ねる。戦闘側のRange Geometryや既存詳細画面レイアウトは変更していない。v67は実機確認待ち。
v68で非投擲系の詳細Range図を説明用のマス整列表示へ修正し、ソード `1×1`、ワイドソード `1×3`、リカバリー10のSELF範囲をグリッド境界へ一致させた。実機確認済み。
v69でチップ固有画像の管理を `M_CHIP.image_path` 方式から `assets/chips/{chip_name}.png` の命名規約方式へ移行した。正式5チップの画像を `キャノン.png / ソード.png / ワイドソード.png / ミニボム.png / リカバリー10.png` へリネームし、`chip-master.js` から画像パス項目を削除。`chip-service.js` がチップ名から画像パスを生成し、`chip_name` 重複をマスタ検証エラーとする。Service Workerのチップ画像個別プリキャッシュ列挙も廃止し、`assets/chips/` は既存の実行時キャッシュで取得する。テストチップの画像流用はサービス層のテスト互換定義へ分離した。v69は実機確認待ち。
v70で最終確認時に残っていたテストチップUIの `chip.imagePath` 直接参照を解消し、正式チップと同じ `BattleNetworkMaster.getChipImagePath()` 経由へ統一した。正式マスタへ画像項目は戻さず、テスト用画像流用もサービス層の互換定義から解決する。正式5チップの日本語ファイル名画像がCUSTOM・詳細で表示されることを実機確認済み。
v71でチップ詳細Range図のP配置を固定左寄せから中央優先へ変更した。7×5グリッドの中央マスを第一候補とし、Range全体が収まる場合は中央配置、見切れる場合のみRange形状・射程・投擲距離・半径に応じて中央に最も近い収容可能マスへPを自動補正する。ソード・ワイドソード・リカバリーは中央配置、キャノン等の長射程は必要に応じて左へ寄せる。攻撃範囲のマス境界一致と正式Range値は変更していない。v71は実機確認済み。
v72で敵HPの最小基盤を `js/combat/enemy-foundation.js` に追加した。敵ごとに `maxHp / hp / isDefeated` を保持でき、`configureHealth()` で既存敵へHPを設定、`applyDamage()` でダメージ減算と0到達判定を行える。テスト敵の最大HPは未決定のため推測値を設定せず、未設定時はHP判定を行わない。撃破削除・HP表示・ダメージ接続は後続フェーズとする。v72はリポジトリ確認済み、実機確認対象の見た目変更はなし。
v73でテスト敵の最大HPを `200` に確定し、敵頭上へ `HP 現在値 / 最大値` の簡易表示を追加した。既存チップマスタの固定威力（キャノン40 / ソード80 / ワイドソード80 / ミニボム50）をv54で確定済みの命中タイミングへ接続し、敵HPを実際に減算する。B攻撃は `js/combat/b-attack-system.js` へ分離し、デフォルトプロファイルをロックバスター、Bタップ威力 `1`、B長押しチャージを通常威力の `10倍`（現行10）として定義した。Bタップ／長押しは別Actionとして保持し、`setActiveProfile()` で将来ソウルユニゾン・スタイルチェンジ・改造等から差し替え可能とした。競合時の優先順位は未確定のため実装していない。撃破削除はまだ行わず、HP0到達判定までとする。v73は実機確認済み。
v74で敵HP表示を原作寄りの残HP数字のみへ変更した。従来の `HP 現在値 / 最大値` と背景付き小ラベルを廃止し、白い太字数字＋黒縁、34pxの角張ったゲーム数字表現へ変更。表示位置比較のため同じ残HPを敵の頭上と足元の両方へ同時表示する。HP内部値・ダメージ処理は変更していない。v74は上下どちらを正式採用するかの実機比較待ち。
v75で敵HP表示位置は足元側を正式採用し、頭上表示を削除した。残HP数字は34pxから40pxへ一回り拡大し、白太字＋黒縁の原作寄り表現を維持する。また実機でロックバスター発射時の画面負荷を確認したため、`projectile-shadow-renderer.js` の弾ごとの `MutationObserver` を廃止し、弾が存在する間だけ1本の `requestAnimationFrame` ループで影を同期する方式へ変更。発射時の `offsetWidth / offsetHeight` 読み取りも固定表示サイズ参照へ置き換え、強制レイアウトを避ける。攻撃力・連射間隔・弾速・命中判定は変更していない。v75のHP表示とロックバスター負荷改善は実機確認待ち。
v76で実機確認によりロックバスター連打時の負荷が残ることを確認。弾影の独立 `requestAnimationFrame` とtransform文字列解析を完全に廃止し、`game.js` の既存弾更新ループから弾本体と影を同時更新する方式へ統合した。またバスター弾の毎フレーム命中判定で `getEnemies()` による凍結スナップショット配列・Bounds生成を行わず、敵内部データを直接参照する `findEnemyIdAtPoint()` を追加した。弾1発ごとの `Set` も不要となるため削除。攻撃力・連射間隔・弾速・HitBox・見た目は変更していない。v76はロックバスター連打時の実機負荷確認待ち。
v77でv76後も実機上のロックバスター連打負荷が大きく残ることを確認し、描画負荷の切り分けを優先した。通常／チャージバスターは床影DOMを一旦持たない構成へ変更し、移動 `box-shadow`、Bボタン押下時の `filter`、チャージ中の発光、敵命中時の `filter + glow`、残HP数字の多重 `text-shadow` をCSS上で無効化した。チャージ完了表示は黒縁・境界線ベースの軽量表現を維持し、キャノン床影は変更していない。攻撃力・連射間隔・弾速・HitBox・HP値は変更していない。v77はロックバスター連打時の実機負荷確認待ち。
v78でv77後もロックバスター連打時の重さがほぼ改善しないことを実機確認したため、Range負荷対策v51と同じ分離方針をB攻撃へ適用した。通常／チャージバスターの移動DOMを5184×2592の巨大 `scene` から外し、`battle` 直下の専用 `busterProjectileLayer` へ分離する。弾のworld座標・弾速・射程・HitBox判定は従来どおり `game.js` が管理し、専用レイヤーは投影後座標の表示だけを担当する。カメラtransformは `scene.style.transform` と同期する。また `camera-zoom-settings.js` が有効な通常実行時は `game.js` 側から同じ `scene.style.transform` を重複更新しないようにし、カメラtransformの二重書き込みも避ける。キャノンは従来どおりscene側を維持する。v78の専用レイヤー分離により、ロックバスター連打時の重さが改善したことを実機確認済み。
v79でCUSTOMゲージMAX到達時にも画面が重く見える実機指摘を受け、MAX状態だけで無限実行されていた `customReadyBlink` の `filter:brightness()` アニメーションを廃止した。MAX到達後はゲージ幅更新自体が停止しているため、状態更新ロジックは変更せず、明るい枠色・ゲージ色・CUSTOM文字色による静的なREADY表示へ置き換えた。外側グローも使用しない。ゲージ蓄積時間10秒・MAX時の再CUSTOM操作・表示位置とサイズは変更していない。v79の静的READY表示によりCUSTOMゲージMAX後の重さが改善したことを実機確認済み。
v80でMAX時の点滅表現を復活させた。ただし負荷原因だったゲージ全体への `filter:brightness()` と外側グローは戻さず、満タンの `customFill` のみを `opacity` 1.0 / 0.42 の2段階で点滅させる。`will-change: opacity` はREADY中のfillだけに限定し、READY解除時は通常へ戻す。枠・CUSTOM文字は静的READY色を維持する。ゲージ蓄積時間10秒・MAX時の再CUSTOM操作・位置・サイズは変更していない。v80の点滅状態でも移動・旋回・ロックバスター連打を含めて動作上の問題がないことを実機確認済み。
v81でプレイヤーHPの最小ランタイム基盤として `js/combat/player-health.js` を追加した。`maxHp / hp / isConfigured / isDefeated` のスナップショットを保持し、`configureHealth()` で最大HP・現在HPを設定、`applyDamage()` でダメージ減算と0到達判定、`clearHealth()` で未設定状態へ戻せる。既存設計では初期最大HPの具体値が未決定のため推測値は設定せず、ロード直後はHP未設定のままとする。敵攻撃との接続、プレイヤーHP表示、撃破処理、リカバリー10の実回復、IndexedDB保存形式は後続フェーズとし、今回の変更では既存戦闘挙動・表示に影響を与えない。v81はリポジトリ確認対象で、実機確認を要する見た目変更はなし。
v82で原作バトル画面を参考にしたプレイヤーHUD基盤を追加した。戦闘画面左上に残HP用ウィンドウ、その直下にココロウィンドウを固定配置する。HP数字は既存 `Orbitron` 系をベースに横幅圧縮・白太字・濃色太縁・詰めた字間で原作寄りの可読性へ調整する。ココロウィンドウは既存作品のキャラクター画像を流用せず、オリジナルの通常状態フェイスと `NORMAL` 状態を示す最小基盤とする。`player-health.js` には購読API `subscribe()` を追加し、将来の被ダメージ／回復時にHUDがイベント駆動で更新できる。初期最大HPは未確定のため推測値を設定せず、未設定時のHUDは `---` を表示する。ココロ状態の種類・遷移・戦闘効果は後続フェーズ。v82は左上HUDの位置・サイズ・フォント・ココロウィンドウ見た目の実機確認待ち。
v83でv82 HUDの実機確認結果を受け、左上HUDが大きすぎてCUSTOMゲージ左端と干渉していたレイアウトを修正した。HP／ココロウィンドウの幅を最大150pxから最大104pxへ縮小し、高さ・枠線・HP数字・ココロ状態文字・内部フェイスも同じ比率感で小型化した。左上固定方針、原作寄せの白太字＋濃色縁フォント、HP未設定時の `---`、ココロ `NORMAL` 基盤は変更していない。CUSTOMゲージ本体の位置・サイズ・挙動にも変更はない。v83はHUDサイズとCUSTOMゲージ非干渉の実機確認待ち。
v84でv83実機確認後もCUSTOMゲージ左端との干渉が残っていたため、HUD側だけを再調整した。HP／ココロウィンドウ幅は最大104pxから最大88pxへ縮小し、HUD左位置を6pxから4pxへ詰めた。高さ・HP数字・ココロ状態文字も比率を保って一段階縮小する。CUSTOMゲージの位置・サイズ・挙動、HP基盤、ココロ状態基盤は変更していない。v84はCUSTOMゲージとの非干渉と視認性の実機確認待ち。
v85でv84のサイズ感を維持したまま、プレイヤーHUD上端をCUSTOMゲージと同じ `top:4px` へ揃えた。HP／ココロウィンドウのサイズ・横位置・表示内容、CUSTOMゲージ本体の位置・サイズ・挙動は変更していない。実機確認でHUDのサイズ感と高さ位置に問題がないことを確認済み。
v86でプレイヤー初期最大HPを `100` に正式決定し、`player-health.js` の初期stateを `maxHp=100 / hp=100` で開始するよう変更した。既存の購読API経由で左上HUDはロード時から残HP `100` を表示する。`configureHealth()` / `clearHealth()` / `applyDamage()` の既存API仕様は変更していない。敵攻撃からの被ダメージ、撃破、リカバリー10の実回復、IndexedDB保存は後続フェーズとする。 実機確認で左上HUDが `100` 表示になっていることを確認済み。
v87で `js/combat/player-damage-system.js` を追加し、敵攻撃側から渡されるHit情報をプレイヤーHitBoxで判定して `BattleNetworkPlayerHealth.applyDamage()` へ接続する共通被ダメージ経路を実装した。点攻撃は `resolvePointHit()`、Range攻撃は既存 `RangeGeometry.intersectsBounds()` を使う `resolveRangeHit()` で処理し、命中時は既存購読APIにより左上HUDへ残HPが自動反映される。敵AI・敵攻撃の生成・攻撃力・攻撃パターン・無敵時間・ノックバック・撃破処理は未確定のため今回決めていない。v87はリポジトリ確認対象で、実際の被ダメージ実機確認は最初の敵攻撃実装後に行う。

v88で最初の実被ダメージ確認用として、テスト敵へ単発の直線弾攻撃を追加した。テスト値は威力 `10`、発射前予兆 `0.7秒`、弾速 `720 world units/秒`、次攻撃まで `2.2秒`。予兆開始時のプレイヤー位置へ照準方向を固定し、静的な射線予兆後に弾を発射する。弾中心のworld座標をv87の `resolvePointHit()` へ渡し、プレイヤーHitBoxへ命中した場合のみHPを減算してHUDへ反映する。CUSTOM／設定／チップ詳細／配置編集表示中は攻撃を停止・取消する。これらの値は被ダメージ経路を実機確認するためのテスト値であり、本番敵の正式バランス値としては確定しない。無敵時間・ノックバック・被弾演出・敵AI本体は後続フェーズとする。v88のテスト敵攻撃と被ダメージ経路は実機確認で問題なし。

v89でキャノンのBehaviorを正式設計どおり非貫通へ修正した。従来は `CANNON_SHOT` がLINE Range内の全敵へ到達時間ごとにダメージを予約していたため、射線上に複数敵がいる場合に後方の敵までダメージが入っていた。`getFirstCannonHit()` で発射方向上の最前面HitBoxだけを取得し、ダメージ対象をその1体に限定する。同時に `game.js` のキャノン弾の飛翔距離も最初の敵までへ短縮し、最初の敵へ到達した時点で弾DOMを消す。キャノンのRange `LINE(5×0.75)`、威力、弾速、表示サイズ、床影は変更しない。実機確認で、キャノン弾が最初の敵で停止し、後方へ貫通しないことを確認済み。

v90で `BattleNetworkPlayerHealth.applyHealing()` を追加し、既存チップ「リカバリー10」を実HPへ接続した。回復量は既存マスタの `RECOVERY=10` をそのまま使用し、現在HPへ10加算する。回復後HPは最大HP100でクランプするため、例えば `80→90`、`95→100` となり100を超えない。HP変化は既存の購読API経由で左上HUDへ即時反映される。HP満タン時は新たな使用制限を追加せず、従来の `useChip()` 消費フローを維持したまま実回復量0となる。撃破後の蘇生は行わない。実機確認で被ダメージ後の10回復、最大HP100での打ち止め、HUD即時反映に問題がないことを確認済み。

v91で原作EXE4の被弾後ショートインビジを参考に、通常被弾が成立した直後から `2秒` の被弾後無敵を `player-damage-system.js` へ追加した。無敵中は `resolvePointHit()` / `resolveRangeHit()` が `INVINCIBLE` として追加ダメージを不成立にし、点攻撃のテスト弾はプレイヤーへ重なっても命中扱いにならず通過する。見た目は `css/player-damage.css` の `opacity` だけを使った段階点滅とし、負荷の大きい `filter` やglowは使用しない。撃破に至る被弾では無敵を開始しない。のけぞり・ノックバックは今回含めない。CUSTOM等で戦闘を一時停止している間に無敵時間自体を停止するかは未確定のため、v91実装では `performance.now()` 基準の実時間で管理する。実機確認で、被弾後に約2秒間プレイヤーが点滅する表示に問題がないことを確認済み。連続被弾防止と無敵中の弾通過は共通被ダメージ経路上で実装済みだが、現テスト敵は次弾までの間隔が長く実機再現しづらいため、高頻度攻撃・複数敵攻撃を導入した段階で再確認する。

v92で通常被弾時の『のけぞり』を位置ノックバックから分離して実装した。実ダメージが成立してHP0にならなかった場合、`BattleNetworkPlayer.beginHitStun()` を通じて現行調整値 `0.3秒` ののけぞり状態へ入る。のけぞり中は移動と A / B / X / Y 操作を無効化し、Bチャージ中なら即キャンセル、ダッシュ中ならその場で中断する。ダッシュの既存クールタイムは維持する。通常ののけぞりではworld座標を押し戻さず、位置ノックバックは将来必要な攻撃ごとの個別性能として残す。v91の被弾後無敵2秒は独立して同時開始し、0.3秒後には点滅中でも操作へ復帰する。実機確認で、0.3秒の操作停止、A/B/X/Y操作不可、Bチャージ／ダッシュ中断、位置非移動、点滅中の操作復帰まで問題ないことを確認済み。

v93でプレイヤーHP0時の最小撃破フローを追加した。`BattleNetworkPlayerHealth.subscribe()` で `isDefeated` を監視し、HP0到達時に `BattleNetworkPlayer.setDefeated(true)` へ接続する。撃破状態では移動・A/B/X/Y・CUSTOM再オープン・CUSTOMゲージ進行を停止し、Bチャージ／ダッシュを中断、既存のプレイヤー弾を消去、Rangeプレビューを非表示にする。v88のテスト敵攻撃は既存の `health.isDefeated` 判定により予兆・弾を消去して以後の攻撃を停止する。HUDは残HP0を維持する。見た目は動作確認用としてプレイヤーを薄表示し、中央へ `DELETED` の仮表示を出すのみで、本番撃破イラスト／演出ではない。撃破後の再戦・ステージ終了等の導線はWave／ステージ設計と合わせるため未確定のままとする。v93は実機確認で、HP0到達・戦闘停止・操作不能・敵攻撃停止・仮DELETED表示まで問題ないことを確認済み。

v94で敵HP0時の共通撃破処理を追加した。`enemy-foundation.js` はHP0の敵を点Hit判定・Range Hit判定・ターゲット取得から除外し、HP表示を停止する。表示は本体をグレー化し `DELETED` を重ねる仮表現とし、本番イラスト／アニメーションへ後から差し替える。Wave接続用として `getActiveEnemies()`、`getBattleState()`（total / active / defeated / allDefeated）、`subscribe()` を追加した。撃破前に発射・発動済みの敵攻撃を消すか継続するかはBehavior側の未確定事項のため、v94では既存挙動を変更しない。実機確認で、敵HP0時の仮DELETED表示、HP表示停止、撃破済み敵のHit／ターゲット除外、新規攻撃停止まで問題ないことを確認済み。

v95で最小Wave基盤を追加した。テスト敵の自動生成を `enemy-foundation.js` から分離し、新規 `wave-system.js` が検証用 `WAVE 1` としてHP200の同一テスト敵2体を生成する。2体・配置は複数敵検証のための仮値で本番仕様ではない。Waveは `getBattleState().allDefeated` を購読し、1体撃破ではACTIVEを維持、全2体撃破時だけCLEAREDへ遷移する。画面上部の `WAVE 1 / WAVE CLEAR` は仮UIで、次Wave自動開始・待機時間・本番敵構成は未確定のまま。v95は実機確認で、2体の独立撃破、1体撃破時のWave継続、全敵撃破時のみWAVE CLEARへ遷移することを確認済み。v96でCUSTOM文字をゲージ中央へ移し、Wave表記との重なりを解消した。v96の表示調整も実機確認済みで、Wave表記位置自体は今後変更可能な暫定配置としている。

v97でWave間CUSTOM接続を追加した。全敵撃破時にWaveを `WAITING_CUSTOM` へ遷移させ、プレイヤー側の `prepareNextWave()` で戦闘を停止、プレイヤー弾を消去し、Wave単位のチップ状態をリセットしてCUSTOM画面を開く。CUSTOM決定後に前Waveの敵を `clearAll()` で削除し、Wave番号を+1して検証用2体を再生成する。固定待機秒数・ウェーブ間強化・本番Wave数／敵構成は未確定のまま。プレイヤーHPと位置は引き継ぎ、自動回復は行わない。またCUSTOM決定はチップ0枚選択でも可能にし、その場合は新規チップを追加せず、表示された未選択手札も消費・破棄しない。

v98でWave切替のテンポを追加した。全敵撃破直後は戦闘を停止して `WAVE CLEAR` を表示し、実機確認用の調整値1.0秒後にCUSTOMを開く。CUSTOM決定が新Wave開始に該当する場合は `WAVE n START` を表示して戦闘停止を維持し、同じく調整値1.0秒後に敵生成・戦闘再開する。初回WAVE 1もCUSTOM決定後にSTART表示を経由する。1.0秒は本番確定値ではなく、実機テンポ確認後に変更可能とする。

新規チップ追加は一旦止め、既存5種類を使用してバトル側の基礎システムを作り込む方針は継続する。

## 実装済み

### フィールド・操作

- 横画面前提。
- 斜め上視点を考慮した正方形フィールド。
- 左スティックによる360度自由移動。
- カメラ追従。
- A: バトルチップ使用。
- B: 差し替え可能なB攻撃スロット。デフォルトはロックバスター（通常1 / チャージ=通常×10）。
- X: 緊急回避ダッシュを実装済み。
- Y: 方向固定トグル。1回タップで現在向き・攻撃方向を固定し、再タップで解除。固定中も移動方向は自由。
- Y方向固定中はYボタンを押下状態の色へ変更する。
- `js/field/field-grid.js` に論理フィールド基盤を分離して追加。
- 論理フィールドは `WORLD_SIZE=3600 / TILE_SIZE=180 / 20×20 / 400マス`。
- 各マスは `row / col / baseTerrain / currentTerrain / walkable` を持ち、初期状態は `NORMAL / walkable=true`。
- `worldToTile / getTile / getTileAtWorld / tileToWorldBounds / tileToWorldCenter / toWorldDistance / forEachTile` の共通APIを追加。
- 地形種別として `NORMAL / POISON / MAGMA / HOLE / ICE / GRASS` を定義済み。個別効果は未実装。
- `js/field/field-grid-renderer.js` が論理グリッド境界を現在の斜め投影へ変換し、20×20の見えるマスをSVGラインで描画する。
- `css/field-grid.css` で従来の繰り返しグリッド模様を無効化し、論理グリッド由来のラインだけを床上へ表示する。
- 見た目の1マスと内部の1マスを1対1で一致させる構成を実装済み。現行180 world unitsのマスサイズは実機確認で良好。
- `game.js` は `BattleNetworkField.WORLD_SIZE` を参照し、フィールドサイズの二重定義を避ける構成へ変更。
- プレイヤーの足元座標 `s.x / s.y` を `getTileAtWorld()` へ渡し、毎フレーム所属マスを更新する。
- ゲーム状態に `tileRow / tileCol / currentTile / currentTerrain` を保持し、将来の床効果判定から利用できる基盤を追加。
- プレイヤーDOMの `data-tile-row / data-tile-col / data-terrain` は所属マスまたは地形が変化した時だけ更新する。画面表示には影響しない。
- v48でbattleの表示サイズを初期化時・resize時にキャッシュし、カメラ計算時の毎フレームレイアウト計測を廃止。
- v48でスティック矩形をpointerdown時にキャッシュし、pointermove中のレイアウト計測を廃止。
- v61でプレイヤーHitBox基盤を追加し、表示サイズと独立管理できるようにした。
- v62のプレイヤー既定表示は108×136px、既定HitBoxは `1.32マス × 1.32マス`。表示サイズは実機確認済み。
- v66でコントローラー配置編集画面からカメラ倍率を `0.20〜0.62`、0.01刻みで変更・保存できる。下限0.20までの操作は実機確認済み。

### B攻撃基盤

- `js/combat/b-attack-system.js` を追加。
- BタップとB長押しを独立したActionとして持つB攻撃プロファイル方式。
- デフォルトプロファイルはロックバスター。通常威力1、チャージ威力は通常×10。
- `setActiveProfile()` / `resetToDefault()` を公開し、将来のソウルユニゾン・スタイルチェンジ・改造から現在のB攻撃を差し替え可能。
- 差し替え要素同士の優先順位は未確定で、現時点では決めない。

### 敵HitBox基盤

- `js/combat/enemy-foundation.js` を追加。
- テスト用簡易敵を1体配置。
- 敵位置はworld座標で保持。
- 表示サイズ・表示位置補正とHitBoxサイズ・HitBox位置補正を独立定義可能。
- `getEnemy / getEnemies` からworld座標上のHitBox境界を取得可能。
- `containsPoint` でworld座標上の点が敵HitBox内か判定可能。
- `intersectsRange / getHitEnemies` でRange Geometryと矩形HitBoxの交差判定が可能。
- v62のテスト敵既定表示は116×140px、既定HitBoxは `1.32マス × 1.32マス`。表示サイズは実機確認済み。
- テスト敵最大HPは200。v75では残HP数字のみを敵の足元側に40pxで表示し、`applyDamage()` により実ダメージを反映する。
- 本番キャラの具体的な表示サイズ・HitBox値は未確定で、個別設定可能な構造を維持する。

### 攻撃Range共通基盤

- `js/combat/range-geometry.js` を追加。
- `LINE / RECT` は、発動位置・360度自由方向・長さ・幅からworld座標上の四角形を生成する。
- `CIRCLE` は指定中心と半径を保持し、点判定・地形判定では連続座標の円として扱う。
- CIRCLEはプレビュー用の多頂点生成を必須とせず、必要な場合だけ任意のsegmentsでポリゴン点を生成できる。
- `containsPoint(shape, x, y)` を実装し、点判定からプレビューと同一Rangeを利用できる構成にした。
- `intersectsBounds(shape, bounds)` を実装し、LINE / RECT / CIRCLEと矩形HitBoxの幾何交差を判定できる。
- `getTilesByCenter(shape)` を実装し、地形変更時にマス中心点ルールで対象マスを取得できる構成にした。
- A押下時にはその時点の向きを正規化して固定し、`lastAttackRange` として同一Range形状を保持する。
- `BattleNetworkCombatRange` から直近攻撃Range・点判定・対象マス取得を参照可能。

### 攻撃範囲プレビュー

- `js/combat/range-preview-renderer.js` を追加。
- `css/range-preview.css` を追加。
- world座標上のRange形状を現在の斜め投影へ変換して表示する。
- LINE / RECTはSVGポリゴン、CIRCLEは投影後のSVG楕円として描画する。
- キャノン・ソード・ワイドソードは旧CSS長方形の回転表示ではなく、新Range形状そのものをプレビューする方式へ移行済み。
- 表示とHit判定で `BattleNetworkRangeGeometry` を共通利用する構成。
- キャノンはv59で `5×0.75`、ソード `1×1`、ワイドソード `1×3`。
- ミニボムは `CIRCLE(radius_tiles=0.75)` を正式採用。
- `js/combat/bomb-preview-renderer.js` を追加し、プレイヤー足元から3マス先の着弾地点までを放物線の投擲予告線として描画する。
- v51でRange SVGを `scene` から分離し、`battle` 直下の専用 `combatPreviewLayer` で描画する構成へ変更。
- v51実機確認で、稀な引っかかりは残るが大幅に改善し、現時点では許容としてRange負荷対応を完了扱いとした。
- v59でRange SVGの表示opacityを0.45へ下げ、攻撃弾・斬撃・爆発等を見やすくした。ミニボム投擲軌道はopacity 0.55。
- v67でチップ詳細画面の範囲図も論理マス縮尺へ統一。7×5の縮小グリッド上へ、RangeマスタとBehaviorマスタの値から射程・幅・半径・投擲距離を描画する。

### CUSTOM

- 戦闘開始時にCUSTOM画面を表示。
- CUSTOM表示中は戦闘停止。
- 5枚を抽選し、画面上には5×2の10枠を表示。
- チップを1回タップで選択、選択済みチップを再タップでキャンセル。
- 決定ボタンとソウルユニゾン用ボタン枠を実装。
- ソウルユニゾンは現時点では未実装。
- カスタムゲージは10秒でMAX。
- v50実機確認で、CUSTOMゲージMAX後の初回タップと蓄積アニメーションの両方を改善済みと確認。
- チップコードを表示し、同一チップまたは同一コードを基準とした複数選択制御を実装。
- `*` コードをワイルドカードとして扱う。
- 属性アイコンは属性ごとの画像素材を共通利用する方式で実装。
- 使用済み・破棄済みチップをWave中に再登場させない管理を実装。

### 現在使用しているバトルチップ

1. キャノン
2. ソード
3. ワイドソード
4. ミニボム
5. リカバリー10

CUSTOM画面には各チップの正式イラストを表示済み。

### チップ画像

現在Git管理されている画像:

- `assets/chips/キャノン.png`
- `assets/chips/ソード.png`
- `assets/chips/ワイドソード.png`
- `assets/chips/ミニボム.png`
- `assets/chips/リカバリー10.png`
- `assets/chips/common/chip_background_standard.jpg`

### チップ使用プロトタイプ

- キャノン: 前方への直進弾。
- ソード: 前方近距離の狭い攻撃範囲。
- ワイドソード: ソードと同程度の射程で横幅を広くした攻撃範囲。
- ミニボム: 前方3マス先へ投擲し、着弾地点を中心に半径0.75マスの円形爆発。
- リカバリー: 回復エフェクトを表示。
- キャノンは `LINE(5×0.75)`、ソードは `RECT(1×1)`、ワイドソードは `RECT(1×3)` のマス単位Range。
- ミニボム投擲距離は `BOMB_THROW.THROW_DISTANCE_TILES=3`。
- ミニボム爆発半径は `CIRCLE / RADIUS_TILES=0.75`。
- キャノンは床面world座標を示す影を表示する。通常ロックバスター / チャージショットの床影はv77の負荷切り分けで一旦撤去中。
- v60で弾の表示中心をworld座標へ合わせ、実サイズから床面とのクリアランスを自動補正する方式へ変更し、実機確認済み。

### IndexedDB v1

- DB名: `BattleNetworkDB`
- DB Version: `1`
- `save_meta`
- `player_progress`
- `owned_chips`
- `folders`
- `folder_chips`

を作成する基盤を実装。

## 設計確定・段階実装中: 論理フィールドと攻撃範囲

詳細は `FIELD_COMBAT_DESIGN.md` をSource of Truthとする。

確定事項:

- ゲームワールドは `3600 × 3600 world units` の正方形を維持する。
- `TILE_SIZE = 180` とし、20×20、合計400マスの論理グリッドを定義する。
- 見た目の1マスと内部の論理マスを1対1で一致させる。
- プレイヤーはマス移動へ変更せず、360度自由移動を維持する。
- プレイヤーの所属マスは足元中央点を基準とする。
- 毒・マグマ等の床効果は所属マスを基準にする。
- 穴等の侵入不可地形は所属マス判定とは分離し、移動HitBoxで判定する。
- 攻撃方向は東西南北／8方向へスナップせず、向いている360度自由方向を使用する。
- Y方向固定中は現在の向き・攻撃方向を保持し、移動方向だけを独立して変更可能とする。
- 攻撃開始時の向きをその攻撃中は固定する。
- 射程・幅・半径等は「何マス分」を設計単位とし、実行時にworld unitsへ変換する。
- 敵へのHit判定はマス単位ではなく連続座標上の攻撃範囲とHitBoxで行う。
- プレイヤーと敵は表示サイズとHitBoxを分離して管理する。
- v62プロトタイプではプレイヤー・テスト敵のHitBox既定値をともに `1.32マス × 1.32マス` とする。
- カメラ倍率は固定値にせず、端末設定として `0.20〜0.62` の範囲を0.01刻みでユーザー調整可能とする。
- 地形変更対象マスは原則としてマス中心点が攻撃範囲内かで判定する。
- Rangeは形状、Behaviorは効果の届け方・発生方法として責務分離する。
- 基本Range Typeは `LINE / RECT / CIRCLE / SECTOR / RING / SELF`。
- 距離系Range Parameterは整数限定にせず小数マスを許可する。
- キャノンは `LINE(5×0.75)`、ソードは `RECT(1×1)`、ワイドソードは `RECT(1×3)`、ミニボムは `CIRCLE(0.75)`、リカバリー10は `SELF` とする。
- ミニボムの投擲距離はRangeではなく `BOMB_THROW` のBehavior Parameterとし、3マスとする。

## 現時点で未実装・未確定

- v62プレイヤー／敵HitBox倍化後の命中感の実機確認。
- v65配置編集バーのレイアウトとY方向固定の実機確認。
- 特殊地形の実ゲーム処理。
- 穴等の侵入不可地形に対する移動HitBox判定。
- 本番敵AI（移動・攻撃選択・複数敵スケジューリング等）。
- 属性相性・倍率・防御等を含む本格ダメージ計算。
- 攻撃ごとの個別性能としての位置ノックバック。
- 本番敵AIに紐づく正式な敵攻撃と攻撃予兆。
- プレイヤー撃破後の本番導線。
- Wave進行の実戦フロー。
- ボス。
- ソウルユニゾン。
- 本格的なプレイヤー・敵・ステージのビジュアル。
- 所持チップ・フォルダ・レギュラーチップとIndexedDBの実ゲーム接続。
- `library_no / capacity_mb / rarity` の正式な原作値確認とマスタ反映。

## 次フェーズ: 敵3種類 正式仕様設計

敵①の基本方向を正式化した。メットール系統の「初心者が攻撃を見て避け、隙に反撃する」役割を参考にしつつ、名称・外見・攻撃表現はオリジナルとする。基本個体は防御なし、移動ありで、待機を挟みながら上下方向主体の短距離移動を行い、攻撃時は停止してプレイヤー方向へ地面を走る非追尾の衝撃波を放つ。攻撃後には短い反撃可能時間を設ける。HP・攻撃力・各種時間・移動速度等の数値は未確定。

敵AI共通仕様として、敵タイプごとに異なるプレイヤー知覚距離を持たせる方針を追加した。知覚範囲外では直接追跡・攻撃を開始せず、一度知覚した後もフィールド全域を無期限に追跡し続けない。知覚開始距離と解除距離を同一にするか、解除距離を別に持たせるかは境界での状態切替を考慮して後続設計で確定する。


v105の別Attack Behavior検証は実機確認済み。1体目の直線射撃と2体目の円形範囲攻撃がそれぞれ独立して動作し、円形予兆の固定地点回避、Movementとの同時実行、被弾判定、撃破停止、Wave切替まで想定どおり成立することを確認した。v102/v103で改善した描画負荷についても再発は確認されていない。

これにより、正式な敵仕様を載せる前の敵AI共通基盤検証はいったん完了扱いとする。現時点で成立確認済みの共通構造は、敵個体ごとの独立行動、同一敵内の `ATTACK` / `MOVEMENT` 独立チャンネル、敵ごとに異なるAttack Behaviorの割当、world座標・HitBox・表示位置の同期更新、CUSTOM / WAVE CLEAR / WAVE START中のAI停止、攻撃描画の専用レイヤー化・DOM再利用である。

v104の `PROTOTYPE_OSCILLATE_MOVEMENT`、v105の `PROTOTYPE_TARGET_AREA`、従来の `PROTOTYPE_STRAIGHT_SHOT` と、それぞれの距離・速度・威力・予兆時間・頻度・見た目はすべて共通基盤検証用であり、本番敵の正式仕様として採用確定しない。

次は初期プロトタイプで使用する雑魚敵3種類について、見た目上の役割だけでなく戦い方が明確に変わるよう、各敵の正式な移動方法、攻撃内容、予兆、HP、攻撃力、行動頻度、行動中の移動継続／停止条件などを設計する。その後、決定した敵を使って通常3Waveの敵数・組み合わせ・配置・難易度進行を正式化する。

敵の正式仕様は、今回までの検証Behaviorをそのまま採用する前提では決めない。必要に応じて新しいMovement / Attack Behaviorを追加し、敵固有の挙動として構成する。

## TODO: バトルビジュアル・フィールドマス整備（2026-09-18）

バトルシステム基盤完成後、通常フィールド（ショップ・ミッション受注等のホーム的な場所）へ着手する前に、モチベーション向上も兼ねてバトル画面上の正式イラスト・フィールドマス表現を整備する。
作業が途中で止まっても再開できるよう、以下の順序と確認事項をTODOとして残す。

### ① プレイヤー

方針:
原作『ロックマンエグゼ4』のバトル中キャラクターに近い2Dゲーム感を維持しつつ、現在の360度自由移動・自由方向攻撃へ対応させる。
まずネクシアの3Dモデルを「表示方式の共通原型」として作成し、そのモデルから16方向（22.5度刻み）の2Dスプライトを生成して実ゲームで評価する。
16方向で不足する場合は32方向（11.25度刻み）を比較し、それでも連続回転が必要と判断した場合にリアルタイム3D表示へ移行する。
ゲーム内部の移動方向・攻撃方向・HitBox・Range Geometryは360度の連続値を維持し、16/32方向方式では見た目だけを最寄り方向のスプライトへ切り替える。

#### Phase 1: 3Dモデル制作の準備

- [x] 1-1. 3Dモデル制作に使用する基準資料を確定する。
  - 基準資料: 2026-09-18にユーザーが提示したネクシア三面図（正面 / 背面 / 右側面）。
  - 3D化で勝手なデザイン変更を行わない。
  - モデリング時に保持する主要特徴:
    - 頭部: 濃青の丸みあるヘルメット、頭頂中央と左右に水色ライン、中央前面の水色パーツ、左右の円形耳部。
    - 顔: 目を露出し、鼻から口元を濃青マスクで覆う。
    - 胸部: 濃紺ボディスーツ上に青い胸装甲、中央に銀 / 水色の円形コア。
    - 肩 / 腕: 丸い肩部、上腕はボディスーツ主体、前腕に大型の青い装甲と水色パーツ、青いグローブ。
    - 胴 / 脚: 胴から脚は濃紺主体、体側から脚へ水色ラインが連続。
    - 膝 / 下腿: 膝前面に水色装甲、大型の青い脛装甲、つま先に水色パーツ。
    - 背面: 胸装甲に対応する背面装甲、ヘルメット後頭部の中央パネル / ライン構成を保持する。
    - 側面: ヘルメット・耳部・胸コア・前腕装甲・脛装甲の前後方向の厚みを三面図に合わせる。
  - 1-1ではデザイン追加・変更は行わず、三面図に存在しない背面 / 斜め形状はPhase 2で三面図との整合を確認しながら決める。
- [x] 1-2. ゲーム上で狙う見た目を確定する。
  - 最終表示の第一候補は、原作『ロックマンエグゼ4』のバトル中キャラクターに近い「小サイズの2Dスプライト感」とする。
  - 3Dモデルそのものの質感をゲーム上へ見せることを目的にせず、3Dモデルは方向・体型・アニメーションを安定して量産するための原型として扱う。
  - 見た目の基準:
    - シルエットを最優先し、小サイズでも頭・胸・前腕・膝・脛の主要形状を判別できること。
    - ネクシア三面図の濃紺 / 青 / 水色 / 銀の色分けを維持し、細かすぎる材質差や写実的な反射表現は持ち込まない。
    - 陰影はキャラクター形状を読み取るための簡潔な段階表現を基本とし、フォトリアルなPBR質感や複雑な環境反射を最終スプライトの目標にしない。
    - 縮小後も目・マスク・胸コア・前腕装甲・膝 / 脛装甲が潰れないことを優先する。
    - 16方向間で輪郭の太さ・明るさ・色味・足元位置が変化しないよう統一する。
    - 待機 / 移動アニメーション中も、同一方向ではキャラクター全体の大きさと足元基準点を安定させる。
    - 背景は透過とし、現在のフィールド・Range・攻撃エフェクト・HUDと重ねて成立すること。
  - 方向表示の考え方:
    - 内部の移動方向 / 攻撃方向は既存どおり360度の連続値を維持する。
    - 16方向方式では見た目だけ22.5度刻みの最寄り方向へ切り替える。
    - 向きの違いを見せるために3D的な遠近変形を強調せず、各方向で同じキャラクターとして読める安定した投影を優先する。
  - 実ゲーム上の評価基準:
    - 原作EXE4系の2Dバトルキャラとして違和感が少ない。
    - スマホ画面・現行カメラ倍率でもキャラクターを識別できる。
    - 方向切替よりもアニメーションと移動が先に目へ入る。
    - Range表示・弾・敵・特殊フィールドマスに埋もれない。
  - この段階では最終スプライトの具体的なピクセル寸法・輪郭処理・使用ツールは固定しない。Phase 1-3で制作手段を決め、Phase 4 / 5で実ゲーム表示を見ながら確定する。
  - 3Dモデルは16 / 32方向スプライト生成と、必要になった場合のリアルタイム3D表示の双方で再利用可能な構造を維持する。
- [x] 1-3. 3Dモデル制作・リグ・アニメーション・書き出しに使用するツールチェーンとファイル形式を決める。
  - 3D制作本体: Blender LTS を使用する。
    - Windows 11で利用可能なLTS版を使用し、プロジェクト途中で不用意にメジャーバージョンを変更しない。
    - モデリング、Armature / Rig、待機・移動アニメーション、16 / 32方向レンダリングを同一Blenderプロジェクトで管理する。
  - Blender作業データの正本: `.blend`
    - メッシュ、マテリアル、Armature、アニメーション、カメラ、照明、レンダー設定を保持する。
  - 3D交換 / リアルタイム3D検証用: glTF 2.0 Binary（`.glb`）
    - Mesh / Material / Skinning / Animationを1ファイルで保持できる形式を採用する。
    - 将来リアルタイム3Dへ移行する場合も、この`.glb`をブラウザ側で読み込める構成を前提とする。
  - 16 / 32方向2Dスプライト書き出し:
    - 透過背景のPNG連番を中間正本とする。
    - 待機 / 移動ごとに方向・フレームを分けて書き出す。
    - 全方向で同一のOrthographic Camera、倍率、照明、足元基準点を使用する。
    - PNG連番からゲーム用spritesheetへまとめる工程はPhase 4で設計する。
  - レンダリング:
    - Blenderのリアルタイム系レンダーを基本とし、透過背景で出力する。
    - 目的は写実的3Dではなく小サイズ2Dスプライトの生成なので、簡潔な色分け・陰影と輪郭の安定を優先する。
  - 3D初期形状の補助手段:
    - ChatGPTの外部連携候補として、2D画像からglTFモデルを生成できる`to3D`が利用可能であることを確認済み。
    - 使用する場合も下地生成専用とし、生成結果をそのまま正式モデルにはしない。
    - ネクシア三面図との差分をBlenderで修正し、三面図をSource of Truthとする。
  - ChatGPT側の実行環境ではBlender CLI / `bpy`は利用できないことを確認済み。
    - そのため`.blend`の直接編集・レンダリングはBlenderを実行可能な別環境が必要。
    - GitHub側のゲームコードへ未検証3D処理を先行追加しない。
  - 2026-09-18、ChatGPT上で`to3D`連携を追加済み。
    - これはChatGPT内の外部3D変換連携であり、ユーザーPCへBlender CLI / `bpy`をインストールしたことを意味しない。
    - `to3D`はネクシア三面図から初期3Dモデルを生成する補助手段として利用する。
  - Blender CLI / `bpy`を使用する工程へ進む場合は、ユーザーPC側へBlenderを別途インストールして実行環境を用意する必要がある。
  - Phase 2の初期モデル生成は、まずChatGPT上の`to3D`で進める。

#### Phase 2: ネクシア3Dモデル

方針:
2026-09-18に`to3D`で正面単体画像から初期glTFを生成して確認したが、基準三面図に対して頭部・胸部・前腕・脚部・全体シルエットの再現精度が不足したため、この生成物は正式モデルの下地として採用しない。
以降はネクシア三面図をSource of Truthとし、Blender上で基本形状を手作業で構築する。
Phase 2は以下を上から1項目ずつ進め、各項目の確認が終わるまで次へ進まない。

- [x] 2-0. Blender作業環境を準備する。
  - Blender 5.2.2 LTSをWindows 11へインストール済み。
  - Blender GUI起動済み。
  - `blender --version` でCLI利用確認済み。
  - `blender --background --python-expr "import bpy; print(bpy.app.version_string)"` で `bpy` 利用確認済み。
  - 確認結果: `5.2.2 LTS`。
  - 完了条件: GUI / CLI / bpy の3点が利用可能。

- [x] 2-1. ネクシア用Blenderプロジェクトを作成する。
  - セットアップスクリプト: `tools/blender/setup_nexia_project.py`。
  - Blender 5.2.2 LTSで実行済み。
  - 実行結果 `NEXIA_PROJECT_SETUP_OK` を確認済み。
  - 生成先: `assets/character/3d/nexia/nexia_model.blend`。
  - 単位: Metric / meters / scale 1.0。
  - 座標系: `+Z=上 / +X=キャラクター右 / -Y=キャラクター正面`。
  - 原点ルール: 足元中央をworld originに置く。
  - キャラクターの絶対身長は現時点では未確定とし、推測値を設定していない。
  - 作業用コレクション: `REFERENCE / BODY / ARMOR / RIG / RENDER`。
  - 同名`.blend`が存在する場合は停止し、意図的に作り直す場合のみ `-- --force` を使用する。
  - 完了条件: 以後のモデリングを同一座標系で進められる空プロジェクトが保存済み。

- [x] 2-2. 三面図をBlenderへリファレンス配置する。
  - 自動配置スクリプト: `tools/blender/setup_nexia_references.py`。
  - デフォルト入力: `assets/character/ネクシア.png`。
  - 入力画像は「正面 / 背面 / 左側面」が左から横並びになっている三面図を使用する。
  - スクリプトは背景とキャラクター色の差から横方向の主要3被写体を検出し、3体を検出できない場合は推測処理を行わず停止する。
  - 3体を検出できた場合のみ、共通の縦範囲・共通サイズで以下を生成する。
    - `assets/character/3d/nexia/reference/front.png`
    - `assets/character/3d/nexia/reference/back.png`
    - `assets/character/3d/nexia/reference/left.png`
  - `REFERENCE` コレクションへ `REF_FRONT / REF_BACK / REF_LEFT` として配置する。
  - 3方向とも足元中央を原点基準とし、同じ表示倍率を使用する。
  - 正面図を正面ビュー、背面図を背面ビュー、左側面図を左側面ビューから確認できる向きで配置する。
  - 画像の縦横比を変更しない。
  - リポジトリルートから以下を実行する。
    - `blender --background --python tools/blender/setup_nexia_references.py`
  - 2026-09-19、Blender 5.2.2 LTSで実行成功。
    - `NEXIA_REFERENCE_SETUP_OK` を確認済み。
    - Source size: `1195x896`。
    - Detected boxes: `[(68, 0, 425, 895), (437, 0, 794, 895), (786, 0, 1143, 895)]`。
    - `REF_FRONT / REF_BACK / REF_LEFT` 作成済み。
    - `assets/character/3d/nexia/nexia_model.blend` 保存済み。
  - 2026-09-19、ユーザー提示の分割後3画像を確認。
    - `front.png / left.png / back.png` はすべて `358x896`。
    - 頭頂・足元の基準位置は揃っている。
    - 肩・肘・手首・股・膝・足首について、モデリング開始を妨げる大きな縦位置ズレは確認されない。
  - 完了条件: 正面 / 背面 / 側面を切り替えても主要関節位置が一致していることを確認済み。

- [x] 2-3. 全身のブロックアウトを作成する。
  - 自動生成スクリプト: `tools/blender/blockout_nexia.py`。
  - `2-2`で確認した `front.png / left.png / back.png` の比率を基準に、全高を `1.0` とした正規化スケールで粗い体型を作る。
  - 絶対身長は未確定のままとし、正規化スケールを最終身長として扱わない。
  - 作成対象:
    - 頭
    - 首
    - 胸郭 / 胴
    - 骨盤
    - 上腕 / 前腕 / 手
    - 太腿 / 下腿 / 足
  - この段階では装甲の細部を作らず、体型と前後厚みだけを合わせる。
  - 左右パーツは同一パラメータをX=0基準で反転生成し、左右差を作らない。
  - `REF_FRONT / REF_BACK / REF_LEFT` が存在しない場合は停止する。
  - リポジトリルートから以下を実行する。
    - `blender --background --python tools/blender/blockout_nexia.py`
  - 2026-09-19、初回確認でブロックアウトの表示スケール不足を確認し、`REF_FRONT` の実表示サイズへ自動フィットするよう修正済み。
  - 2026-09-19、正面 / 左側面の再確認で以下を調整済み。
    - 前腕を素体として細く調整。
    - 下腿を素体として細く調整。
    - 足を小さくし、側面での過度な横長形状を抑制。
    - 胸郭 / 腹 / 骨盤の幅と重なりを調整し、球を積んだような見え方を緩和。
  - 2026-09-19、ユーザー提示の正面 / 左側面スクリーンショットで最終確認。
    - 全高と頭位置はリファレンスに概ね一致。
    - 肩幅 / 胴幅 / 腕長 / 脚長はブロックアウトとして大きな破綻なし。
    - 前腕 / 下腿 / 足は「装甲を除いた素体」として次工程へ進める太さに収まっている。
    - 側面の頭・胴・腕・脚の前後厚みも、粗形状として大きな破綻なし。
  - 完了条件: 装甲なしの粗形状でも、正面・側面の体型比率が基準三面図と概ね一致していることを確認済み。

### Phase 2-A: AIマルチビュー3D生成ルート（優先）

目的:
手作業で三面図へ合わせ続ける時間を削減するため、正面 / 背面 / 左側面の3枚を直接参照できるAI 3D生成を先に試し、使える品質なら以後の正式モデルのベースに採用する。

使用候補:
- Tencent `Hunyuan3D-2mv`。
- 公式Hugging Face SpaceのMultiView入力を第一候補とする。
- 入力は `front.png / back.png / left.png`。Rightは現時点では未用意のため空欄とする。
- 最初はTextureではなくShape生成を優先し、三面図との形状一致を評価する。
- AI生成結果が不採用でも、既存の手作業 `nexia_model.blend` とスクリプト群は削除せずフォールバックとして残す。
- 2026-09-20、従来 `right.png / REF_RIGHT` としていた側面図は実際にはキャラクターの左側面であることを確認。`left.png / REF_LEFT` へ修正し、旧 `right.png` は再生成時に削除する。

- [x] 2-A-1. Hunyuan3D-2mvへ3方向画像を入力する。
  - Front: `assets/character/3d/nexia/reference/front.png`
  - Back: `assets/character/3d/nexia/reference/back.png`
  - Left: `assets/character/3d/nexia/reference/left.png`
  - Right: 未指定。
  - `Gen Shape` を実行済み。
  - 2026-09-21、Hunyuan3D-2mvのGenerated Mesh画面で1体の3D Shape生成を確認。
    - 全身比率、ヘルメット、胸部、前腕装甲、脚装甲、足の主要シルエットを概ね取得できている。
    - 現時点では「修正ベースとして有望」。正式採用判定はGLBをBlenderへ取り込んで三面図比較後に行う。
  - 完了条件: 3方向画像から1体の3D Shapeが生成される。達成済み。

- [ ] 2-A-2. 生成ShapeをGLBで保存する。
  - 保存先予定: `assets/character/3d/nexia/ai/hunyuan3d-2mv.glb`。
  - リポジトリへ追加する前に、生成結果を目視確認する。
  - 完了条件: Blenderへ取り込めるGLBを取得済み。

- [ ] 2-A-3. AI生成モデルをBlenderへ別オブジェクトとして取り込む。
  - 既存の手作業モデルを上書きしない。
  - AI候補用コレクションを分離する。
  - 既存 `REFERENCE` の正面 / 背面 / 左側面と同じ座標系へ合わせる。
  - 完了条件: 三面図とAIモデルを同じBlenderファイル内で比較可能。

- [ ] 2-A-4. 三面図との一致度を評価する。
  - 全高 / 頭身。
  - 頭部・ヘルメット外形。
  - 胸・腰の比率。
  - 腕長 / 脚長。
  - 前腕装甲 / 脛装甲 / 足。
  - 正面 / 背面 / 左側面で重大な形状破綻がないか確認する。
  - 完了条件: 「そのまま採用 / 修正ベースとして採用 / 不採用」のいずれかをユーザー確認で確定する。

- [ ] 2-A-5. 採用ルートを確定する。
  - そのまま採用: AIモデルを正式ベースとしてトポロジー・マテリアル・リグ工程へ進む。
  - 修正ベースとして採用: AIモデルの必要箇所だけBlenderで修正する。
  - 不採用: 下記の手作業2-4以降へ戻る。
  - 完了条件: 以後の3D制作ルートが1つに確定している。

### Phase 2-B: 手作業モデリングルート（AI不採用または補正が必要な場合）

- [ ] 2-4. 頭部・ヘルメットを作成する。（現在一時停止。Phase 2-Aの結果を先に評価する）
  - 自動生成スクリプト: `tools/blender/model_nexia_head.py`。
  - 2-3で確定した正規化頭部比率と `nexia_blockout_fit_scale / Z offset` を基準に、頭部形状を以下の別オブジェクトとして追加する。
    - ヘルメット本体。
    - 顔の露出部。
    - 濃青マスク。
    - 頭頂中央の上下パーツ。
    - 左右の水色ライン。
    - 左右の円形耳部（外周 / 中間 / 中央）。
  - `-Y` を正面として既存座標系を維持する。
  - 頭部確認中は `BLOCKOUT_HEAD` を非表示にし、詳細頭部を優先表示する。
  - 既存の胴体 / 腕 / 脚ブロックアウトは変更しない。
  - 自動保存時の `.blend1 / .blend2` は生成しない。
  - リポジトリルートから以下を実行する。
    - `blender --background --python tools/blender/model_nexia_head.py`
  - 実行成功時に `NEXIA_HEAD_SETUP_OK` が表示されることを確認する。
  - 2026-09-19 初回生成確認:
    - ヘルメットが球体に見えすぎる。
    - 顔露出部が横長板状、マスクが四角い。
    - 頭頂中央パーツが浮いて見える。
    - 左右ラインが角のように突出し、背面へ十分つながっていない。
    - 耳部が小さく、ヘルメットから浮いて見える。
  - 上記確認を受け、以下を修正済み。
    - ヘルメットを前後方向に少し扁平化し、中心を後方へずらして球体感を抑制。
    - 顔露出部とマスクを台形断面へ変更。
    - 目周りを縁取る青い眉ガードを追加。
    - 頭頂中央パーツをヘルメット面へ寄せ、背面中央の水色パーツも追加。
    - 左右ラインを前面から頭頂・後頭部まで連続する曲線へ修正。
    - 耳部を大型化し、ヘルメットへ食い込む位置へ移動。
  - 2026-09-19 再生成確認で頭部全体が巨大化するスケール異常を確認。
    - 実行ログで `BLOCKOUT_HEAD.dimensions = (1.999999, 1.999999, 2.0)` が入力されていたことを確認。
    - `BLOCKOUT_HEAD.dimensions` を頭部サイズのSource of Truthとして使う方式を廃止。
    - 2-3と同じ正規化頭部比率（幅0.234 / 奥行0.226 / 高さはY=18〜198由来）と、保存済みの `nexia_blockout_fit_scale / nexia_blockout_fit_z_offset` から頭部サイズ・中心位置を再計算する方式へ修正済み。
    - 2026-09-20、スケール修正版の正面 / 左側面 / 背面を確認し、頭部全体のサイズ・位置は正常化したことを確認。
    - 一方で、ヘルメット本体は依然として球体感が強く、三面図の前頭部 / 側頭部 / 後頭部の面構成を再現できていないため、2-4は未完了。
    - 正式形状工程へ移行し、`HEAD_HELMET_SHELL` をUV球ベースから断面制御した専用メッシュへ置換済み。
      - 下側を絞る。
      - 前面奥行きを浅くする。
      - 後頭部を前面より張らせる。
      - 頭頂を球よりやや平たくする。
    - 2026-09-20、正式ヘルメット外形の初回確認で以下を確認。
      - 球体感は改善。
      - 正面 / 背面は方向性OKだが、中〜下部がやや細い。
      - 左側面は前後厚み不足。
      - 後頭部の張り出し不足。
      - 頭頂高さは概ね維持してよい。
    - 上記を受け、ヘルメット断面パラメータのみ再調整済み。
      - 中〜下部の横幅を拡大。
      - 前後方向の厚みを増加。
      - 後頭部側を前面より強く張り出す。
      - 頭頂リングは変更せず高さ感を維持。
    - 現在はヘルメット外形2回目の再確認待ち。
  - 実行後、Blender GUIで正面 / 左側面 / 背面をリファレンスと重ねて確認する。
  - 目そのもの・細かい質感は、頭部基本形状が確定するまで追加しない。
  - 完了条件: 正面 / 側面 / 背面のいずれでも三面図の頭部シルエットが読み取れる。

- [ ] 2-5. 胸部・胴体を作成する。
  - 胸装甲の外形を作る。
  - 中央の円形コアと周囲の銀色パーツを作る。
  - 首周辺から胸装甲への接続形状を作る。
  - 背面装甲を三面図に合わせて作る。
  - 腹部は装甲化せず、三面図どおりボディスーツ主体を維持する。
  - 完了条件: 正面と背面で胸装甲の大きさ・高さ・厚みが基準図と一致する。

- [ ] 2-6. 腕・手・前腕装甲を作成する。
  - 肩から上腕はボディスーツ主体で作る。
  - 前腕の大型青装甲を作る。
  - 前腕外側の水色パーツを作る。
  - 手と指の基本形状を作る。
  - 腕を下ろした基準姿勢で三面図と比較する。
  - 完了条件: 正面 / 側面で腕の太さ、前腕装甲の長さ・厚み、手の位置が一致する。

- [ ] 2-7. 脚・足・下腿装甲を作成する。
  - 太腿はボディスーツ主体で作る。
  - 膝前面の水色装甲を作る。
  - 大型の青い脛装甲を作る。
  - 足首から足の基本形状を作る。
  - つま先の水色パーツを作る。
  - 側面図で脛・踵・つま先の前後方向厚みを合わせる。
  - 完了条件: 正面 / 側面で脚長、膝位置、脛装甲、足の大きさが基準図と一致する。

- [ ] 2-8. ボディスーツの水色ラインを3D形状またはマテリアル境界として再現する。
  - 肩付近のライン。
  - 胴体側面から脚へ続くライン。
  - 三面図上で存在しないラインを追加しない。
  - 完了条件: 正面 / 背面 / 側面でラインの始点・終点・連続性が基準図と一致する。

- [ ] 2-9. 全身比率を三面図と再比較して調整する。
  - 頭身。
  - 肩幅。
  - 胸幅 / 腰幅。
  - 腕長。
  - 脚長。
  - 前腕装甲・脛装甲のボリューム。
  - 左右対称性。
  - 完了条件: 正面 / 背面 / 側面の3方向で大きな比率差が残っていない。

- [ ] 2-10. 斜め方向の形状を確認・調整する。
  - 22.5度。
  - 45度。
  - 67.5度。
  - 三面図から直接見えない接続部は、既存3方向との整合性を崩さない範囲で決める。
  - 勝手な装飾追加は行わない。
  - 完了条件: 16方向スプライト化した際に大きな形状飛びが起きない外形になっている。

- [ ] 2-11. トポロジーを整理する。
  - 不要な面・内部面を削除する。
  - 法線方向を確認する。
  - 目立つ非多様体箇所や破綻を修正する。
  - 後続のArmature変形に必要な関節周辺へ適切なエッジを確保する。
  - 過剰な高密度化は避ける。
  - 完了条件: リグ作業へ移れるクリーンな基本メッシュになっている。

- [ ] 2-12. 基本配色・マテリアルを設定する。
  - 濃紺ボディスーツ。
  - 青装甲。
  - 水色アクセント。
  - 銀色パーツ。
  - 目 / 肌露出部。
  - 写実的な金属反射より、Phase 1-2で確定した小サイズ2Dスプライト向けの色分けを優先する。
  - 完了条件: 小さく表示しても主要パーツの色分けが識別できる。

- [ ] 2-13. 360度ターンテーブル確認を行う。
  - 正面。
  - 22.5度 / 45度 / 67.5度。
  - 側面。
  - 後方斜め。
  - 背面。
  - 左右対称側。
  - 頭部・胸部・前腕・膝・脛・足の破綻を確認する。
  - 完了条件: 16方向すべての静止画で重大な形状破綻がない。

- [ ] 2-14. 基本モデルをユーザー確認する。
  - 正面 / 背面 / 側面 / 斜めの比較画像を用意する。
  - 基準三面図と並べて確認する。
  - 指摘があれば該当Phaseへ戻って修正する。
  - 完了条件: ユーザーOK後のみPhase 3「リグ・最低限アニメーション」へ進む。

#### Phase 3: リグ・最低限アニメーション

- [ ] 3-1. ボーン / リグを設定する。
- [ ] 3-2. 通常待機アニメーションを作成する。
- [ ] 3-3. 移動アニメーションを作成する。
- [ ] 3-4. 3Dモデル上で待機・移動時に装甲や関節が破綻しないか確認する。
- [ ] 3-5. ユーザー確認後、16方向検証用アニメーションを確定する。

#### Phase 4: 16方向2Dスプライト生成

- [ ] 4-1. 16方向の基準角度を22.5度刻みで固定する。
- [ ] 4-2. 全方向で同じカメラ・倍率・照明・キャラクター基準位置を使用する。
- [ ] 4-3. 通常待機を16方向へ書き出す。
- [ ] 4-4. 移動を16方向へ書き出す。
- [ ] 4-5. 透過背景・フレームサイズ・足元基準点・命名規約を統一する。
- [ ] 4-6. 原作EXE4に近い小サイズ表示へ縮小した際の視認性を確認する。
- [ ] 4-7. 必要なら縮小後の輪郭・色・ピクセル感を調整する。

#### Phase 5: 16方向表示の実ゲーム接続

- [ ] 5-1. プレイヤーの連続360度向きから、表示用16方向へ量子化する処理を追加する。
- [ ] 5-2. 表示方向のみ16方向へ丸め、内部の移動方向・攻撃方向は360度のまま維持する。
- [ ] 5-3. 現在のCSS仮プレイヤー表示を16方向スプライト表示へ差し替える。
- [ ] 5-4. 既存HitBox・移動速度・敵AI・攻撃処理・Range Geometryを変更しない。
- [ ] 5-5. 方向固定（Y）中も、攻撃方向と表示方向の関係が自然か確認する。
- [ ] 5-6. 待機 / 移動の切替が自然か確認する。
- [ ] 5-7. キャラクターの足元world座標とスプライト基準点がずれないか確認する。
- [ ] 5-8. キャッシュversionを使用するJS/CSSを変更した場合は、読み込み側versionも更新する。

#### Phase 6: 16方向 実機評価

- [ ] 6-1. スマホ実機で360度移動させ、方向切替の見え方を確認する。
- [ ] 6-2. 原作EXE4風の見た目が維持できているか確認する。
- [ ] 6-3. 22.5度刻みの切替が目立つか確認する。
- [ ] 6-4. 移動中のアニメーションが不自然にリセットされないか確認する。
- [ ] 6-5. カメラ倍率変更時も視認性が保てるか確認する。
- [ ] 6-6. FPS・入力遅延・描画負荷を確認する。
- [ ] 6-7. 16方向を正式採用できるか判定する。

#### Phase 7: 16方向で不足した場合のみ比較

- [ ] 7-1. 16方向の問題点を記録し、方向数を増やすことで解決する問題か判断する。
- [ ] 7-2. 必要な場合のみ32方向（11.25度刻み）を書き出す。
- [ ] 7-3. 同一条件で16方向 / 32方向を実ゲーム比較する。
- [ ] 7-4. 32方向で十分なら32方向方式を正式採用する。
- [ ] 7-5. 32方向でも連続回転の違和感が残る場合のみ、リアルタイム3D表示を検討する。

#### Phase 8: リアルタイム3Dが必要な場合のみ

- [ ] 8-1. 既存DOM/CSS/SVG戦闘画面を維持したまま、プレイヤー表示だけ3D化できる構成を設計する。
- [ ] 8-2. 3Dモデルの位置・向きを既存world座標 / 360度向きへ同期する方法を検証する。
- [ ] 8-3. スマホ実機の描画負荷を検証する。
- [ ] 8-4. 16/32方向方式と見た目・性能・実装負荷を比較する。
- [ ] 8-5. 比較結果を確認してから正式な表示方式を決定する。

#### Phase 9: 表示方式確定後のプレイヤー状態拡張

- [ ] 9-1. チップ攻撃アニメーションを追加する。
- [ ] 9-2. ロックバスター / チャージショットの表示を追加・調整する。
- [ ] 9-3. ダッシュ表示を追加する。
- [ ] 9-4. 被弾表示を追加する。
- [ ] 9-5. 無敵点滅表現を追加する。
- [ ] 9-6. 撃破表示を追加する。
- [ ] 9-7. 正式素材に合わせて表示サイズを調整する。
- [ ] 9-8. HitBoxは表示サイズと独立管理を維持し、変更が必要な場合は別途実機確認して確定する。
- [ ] 9-9. 全状態を実機確認し、①プレイヤーを完了扱いにする。

### ② メットール

- [ ] 現在の通常敵（メットール想定）へ正式イラストを設定する。
- [ ] 実装前に必要な表示状態を整理する。
  - 待機
  - 移動
  - 攻撃予兆
  - 攻撃
  - 被弾
  - 撃破
- [ ] 正式素材に合わせて表示サイズ・HitBoxの調整要否を確認する。
- [ ] 現在のMovement / Attack Behaviorを変更せず、まず表示差し替えとして成立するか確認する。
- [ ] 実機で移動・攻撃予兆・攻撃・被弾・撃破表示を確認する。

### ③ フィールドマス

第1弾として以下を実装対象とする。

- [ ] 通常
- [ ] 穴空き（復活あり）
- [ ] 穴空き（復活なし）
- [ ] ひび割れ
- [ ] 毒
- [ ] マグマ
- [ ] アイス
- [ ] 草

あわせて以下を整理する。

- [ ] 自陣 / 敵陣を特殊マス上でも識別できる見た目にする。
- [ ] 通常 → ひび割れ → 穴など、必要な地形状態遷移を決める。
- [ ] 穴空き（復活あり）の復活条件・復活時間・復活時表現を決める。
- [ ] 各特殊マスのゲーム効果を個別に決める。
  - 毒
  - マグマ
  - アイス
  - 草
  - ひび割れ
  - 穴
- [ ] 移動可能範囲・攻撃範囲等のハイライトと特殊マス画像を重ねても視認性が保てるか確認する。
- [ ] 特殊マス実装時も、既存の自由移動・移動HitBox・Range Geometryを壊さないことを確認する。

### ④ 攻撃が「空中を通る攻撃 / 地面を通る攻撃」かを判別する共通設定の確認・設計

目的:
穴マスに対して各攻撃が「通過する / 穴で止まる / その他の個別挙動」のどれになるかを、攻撃ごとに明示的に決められるようにする。

2026-09-18時点のコード確認結果:

- チップのRange / Behavior共通マスタには、攻撃を一律に「空中攻撃 / 地面攻撃」と分類する共通設定値は見当たらない。
- `ENEMY1_GROUND_SHOCKWAVE` はBehavior名として地面攻撃であることを表しており、`enemy1-shockwave.js` 内でHOLE判定を個別実装している。
- `BOMB_THROW` には `IGNORE_INTERMEDIATE_OBSTACLES=true` があるが、これは投擲経路上の障害物を無視する設定であり、攻撃全体を「空中 / 地面」に分類する共通設定ではない。

TODO:

- [ ] 攻撃共通マスタ / Behaviorへ「空中を通るか、地面を通るか」を表す設定が必要か正式に決める。
- [ ] 必要と判断した場合のみ、設定名・責務・保持場所を設計する。推測で項目追加しない。
- [ ] その設定またはBehavior仕様を基準に、既存攻撃を1つずつ確認する。
- [ ] 各攻撃について穴マスとの関係を「通過 / 停止 / 個別処理」で確定する。
- [ ] プレイヤー攻撃・敵攻撃の双方で同じ考え方を利用できるか確認する。
- [ ] 仕様確定後にのみgameplay codeへ反映する。
