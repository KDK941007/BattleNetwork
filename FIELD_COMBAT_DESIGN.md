# BattleNetwork フィールド・攻撃範囲設計

最終更新: 2026-08-25
Status: Design Confirmed / Partial Implementation

このドキュメントは、戦闘フィールドの論理マス、プレイヤーとマスの関係、バトルチップの攻撃範囲判定に関する Source of Truth とする。
フィールド描画、地形効果、移動衝突、攻撃範囲を実装・変更する場合は本設計を基準にする。

## 1. 基本思想

BattleNetworkは「マス制移動ゲーム」にはしない。

- プレイヤー移動は360度自由移動を維持する。
- プレイヤーの向きも360度自由方向を維持する。
- フィールド内部には論理マスを定義する。
- マスは移動を固定するためではなく、地形・攻撃距離・地形変更等のゲーム世界共通単位として利用する。
- 見えているマスと内部の論理マスは1対1で一致させる。

目的は以下。

1. マグマ、毒、穴等の特殊地形をマス単位で管理する。
2. バトルチップの射程・幅を「何マス分」として定義できるようにする。
3. 自由移動・360度攻撃と、地形の離散的な管理を両立する。

## 2. 論理フィールド

現在のゲームワールドは `3600 × 3600 world units` の正方形である。

正式な論理グリッドは以下とする。

- `TILE_SIZE = 180 world units`
- `GRID_COLS = 20`
- `GRID_ROWS = 20`
- 合計400マス

`3600 / 180 = 20` となるため、フィールド端に半端なマスを作らない。

各マスは `row / col` で識別する。

```text
row: 0 - 19
col: 0 - 19
```

ワールド座標から所属マスを求める基本式は以下。

```text
col = floor(worldX / TILE_SIZE)
row = floor(worldY / TILE_SIZE)
```

フィールド端では0～19へクランプする。

## 3. 描画と論理マス

従来の床に表示されていた繰り返し模様は装飾であり、論理マスではなかった。

2026-08-25に `js/field/field-grid.js` と `js/field/field-grid-renderer.js` を追加し、20×20の論理マス境界を現在の斜め投影へ変換して床へ描画する方式へ切り替えた。
`css/field-grid.css` で旧繰り返し模様を無効化し、論理グリッド由来のラインのみ表示する。

原則:

```text
画面に見える1マス = 内部データ上の1マス
```

斜め上視点は表示上の投影であり、論理フィールド自体は180×180の正方形マスとして扱う。
端末解像度・カメラ位置・カメラ倍率によって論理マスサイズを変えない。

現行 `TILE_SIZE=180` の床表示は実機確認で良好と判断した。将来、敵・攻撃範囲・特殊地形を載せた段階で必要になれば調整可能とする。

## 4. プレイヤーとマス

### 4.1 自由移動

プレイヤーはマス中央へスナップさせない。
現在の360度自由移動を維持する。

### 4.2 所属マス

プレイヤーの所属マスは、原則として「足元中央点」が入っている1マスとする。

床効果の基本判定は所属マスを使用する。

例:

- 毒床
- マグマ床
- 草地
- 氷床等の床効果

キャラクター画像の一部が隣マスへ重なっただけでは所属マスを変更しない。

2026-08-25に `game.js` を論理グリッドへ接続し、プレイヤー足元座標 `s.x / s.y` から `BattleNetworkField.getTileAtWorld()` を使用して毎フレーム所属マスを取得する処理を実装した。
ゲーム状態には `tileRow / tileCol / currentTile` を保持する。
デバッグ確認用として、プレイヤーDOMへ `data-tile-row / data-tile-col / data-terrain` を反映する。画面上の表示は変更しない。

### 4.3 移動衝突

所属マス判定と移動衝突判定は分離する。

- 床効果: 足元中央点による所属マス判定。
- 穴・侵入不可地形等: プレイヤーの移動用HitBoxによる面判定。

侵入不可地形へ斜めに接触した場合は、可能であればX/Y方向を分けて判定し、完全停止ではなく地形に沿って滑れる移動を目標とする。

## 5. 地形データ

マスは将来的に以下のような地形を持てる構造とする。

初期想定:

- `NORMAL`
- `POISON`
- `MAGMA`
- `HOLE`
- `ICE`
- `GRASS`

ただし、全種類を最初から実装する必要はない。

最低限、マスごとに以下を表現できる設計とする。

```text
row
col
terrain
walkable
```

一時的な地形変更を考慮し、将来的には元地形と現在地形を分離可能な構造とする。

例:

```text
baseTerrain
currentTerrain
duration
```

具体的な地形効果・持続時間・属性相互作用は別途設計する。

## 6. 攻撃範囲の基本思想

攻撃方向を東西南北や8方向へスナップさせない。

バトルチップは、プレイヤーが向いている360度自由方向をそのまま攻撃方向として使用する。

一方、射程や幅の設計単位には論理マスを使用する。

つまり、

```text
方向 = 連続値（自由角度）
距離・幅 = マス数
実際の当たり判定 = 連続ワールド座標
```

とする。

「マスを攻撃先の固定セルとして使う」のではなく、「ゲーム世界の距離単位として使う」。

## 7. マス数からワールド距離への変換

Rangeマスタでは、原則として射程・幅・半径等をマス単位で表現する。

実行時に以下でワールド距離へ変換する。

```text
worldDistance = tileDistance × TILE_SIZE
```

`TILE_SIZE = 180`。

距離系パラメータは整数に限定せず、小数マスを許可する。
例: `0.25マス = 45 world units`、`0.5マス = 90 world units`。

例:

```text
キャノン
射程: 5マス
幅: 0.5マス
→ 射程 5 × 180 = 900 world units
→ 幅 0.5 × 180 = 90 world units
```

共通変換API `BattleNetworkField.toWorldDistance(tileDistance)` は実装済み。
2026-08-25にキャノン・ソード・ワイドソードを新Range Parameterへ移行し、実ゲーム側もこの変換を通す構成へ接続した。

ミニボムはBehavior側 `THROW_DISTANCE_TILES=3` とし、プレイヤーから3マス先、つまり540 world units先を着弾中心とする。
爆発半径は360度自由移動での当てやすさを考慮し、`radius_tiles=0.75`、つまり135 world unitsを正式採用する。

## 8. 360度攻撃範囲

攻撃開始時にプレイヤーの向きベクトルを取得する。

```text
direction = normalized(playerFacing)
```

攻撃の基本原点は、A押下時点のプレイヤー足元座標とする。

例えば射程5マスなら、攻撃終点は概念上以下となる。

```text
endX = playerX + directionX × (5 × TILE_SIZE)
endY = playerY + directionY × (5 × TILE_SIZE)
```

プレイヤーが斜めを向いていても、その角度のまま範囲を回転させる。

攻撃開始後にスティック入力が変わっても、その攻撃の方向は途中で回転させない。
A押下時点の向きをその攻撃の基準方向として固定する。

2026-08-25に `js/combat/range-geometry.js` を追加した。
`LINE / RECT` は、発動原点・正規化した方向ベクトル・長さ・幅からworld座標上の四角形を生成する。
`CIRCLE` は、発生中心と半径を連続world座標として保持し、Hit判定や地形判定に使用する。プレビュー用ポリゴン点は必須としない。

ゲーム側はA押下時に生成したRange形状を `lastAttackRange` として保持し、攻撃方向をその時点で固定する。

## 9. 敵への当たり判定

敵への攻撃判定は「敵がどのマスにいるか」だけでは判定しない。

敵HitBoxと、回転済みの攻撃範囲を連続ワールド座標上で判定する。

これにより、自由移動中の敵・プレイヤーに対して自然な360度アクションの当たり判定を維持する。

マスは射程・幅等の設計単位として使用し、敵Hit判定そのものを離散化しない。

`BattleNetworkRangeGeometry.containsPoint(shape, x, y)` を共通Range判定APIとして実装済み。
敵HitBoxとの交差は `BattleNetworkRangeGeometry.intersectsBounds(shape, bounds)` を使用し、プレビューとHit判定で同一Range形状を参照する。

ゲーム側からは `BattleNetworkCombatRange` を通じ、直近攻撃Rangeと点判定を参照可能とする。

## 10. 地形への攻撃判定

地形変更系のチップでは、360度自由方向の攻撃範囲と論理マスを組み合わせる。

基本処理:

1. プレイヤーの向きから連続座標上の攻撃範囲を生成する。
2. 20×20の論理マスを対象に範囲との関係を判定する。
3. 対象となったマスへ地形変更を適用する。

範囲の端がマスへわずかに触れただけで対象が不安定に増減することを避けるため、地形変更対象マスは原則として「マス中心点が攻撃範囲内に入っているか」で判定する。

この中心点ルールは地形変更に対する基本方針とし、特殊チップで別ルールが必要になった場合のみ個別定義する。

`BattleNetworkRangeGeometry.getTilesByCenter(shape)` を実装済みで、この中心点ルールに従う対象マス取得に使用できる。

## 11. Range形状と責務

Rangeは「どの形の範囲に効果が届くか」だけを表す。
投擲、弾速、貫通、発生地点など「どうやってその範囲へ効果を届けるか」はBehavior側の責務とする。

正式な基本Range Typeは以下。

| Range Type | 意味 | 主なパラメータ |
| --- | --- | --- |
| `LINE` | 前方へ伸びる直線・射線 | `length_tiles`, `width_tiles` |
| `RECT` | 前方へ伸びる長方形 | `length_tiles`, `width_tiles` |
| `CIRCLE` | 指定中心からの円形 | `radius_tiles` |
| `SECTOR` | 指定中心・方向からの扇形 | `radius_tiles`, `angle_deg` |
| `RING` | 指定中心からの円環 | `inner_radius_tiles`, `outer_radius_tiles` |
| `SELF` | 使用者自身 | パラメータなし |

既存5チップで当面使用するのは `LINE / RECT / CIRCLE / SELF`。
`SECTOR / RING` は将来拡張用として定義可能な基本形状とする。

### 11.1 LINE

- 前方の射線・細い直線攻撃用。
- `length_tiles`: 射程。
- `width_tiles`: 射線幅。
- 最初の敵で止まる、貫通する、壁で止まる等はBehaviorで定義する。

例: キャノン

```text
Range = LINE
length_tiles = 5
width_tiles = 0.5
```

### 11.2 RECT

- 前方の長方形を一括範囲として扱う。
- `length_tiles`: 前方方向の長さ。
- `width_tiles`: 攻撃方向に対して左右均等に広がる幅。
- 幅は論理マスを直接選択する意味ではなく、連続空間上の距離として扱う。

例:

```text
ソード
Range = RECT
length_tiles = 1
width_tiles = 1

ワイドソード
Range = RECT
length_tiles = 1
width_tiles = 3
```

### 11.3 CIRCLE

- 指定された発生中心を基準とした円形範囲。
- `radius_tiles`: 半径。
- Range自体は発生中心を決めない。
- 自分中心、着弾地点、敵位置等の「どこを中心に発生させるか」はBehavior側で決める。

ミニボムは以下で正式確定する。

```text
Behavior = BOMB_THROW
throw_distance_tiles = 3
Range = CIRCLE
radius_tiles = 0.75
```

投擲予告はRange形状とは分離し、Behaviorの見せ方としてプレイヤーから着弾地点までの放物線を表示する。

### 11.4 SECTOR

- 将来の扇状攻撃用。
- `radius_tiles`: 射程。
- `angle_deg`: 扇形の開き角度。

### 11.5 RING

- 将来のドーナツ型攻撃用。
- `inner_radius_tiles`: 内側半径。
- `outer_radius_tiles`: 外側半径。

### 11.6 SELF

- 使用者自身のみを対象とする。
- Range Parameterは不要。
- リカバリー10等に使用する。

## 12. RangeとBehaviorの分離

責務を以下で固定する。

```text
Range
= どこに効果が届くか / 形状

Behavior
= どうやって効果を発生させるか / 届けるか
```

例:

```text
キャノン
Range: LINE 5マス / 幅0.5マス
Behavior: CANNON_SHOT
→ 弾速、非貫通等はBehavior

ミニボム
Range: CIRCLE 0.75マス
Behavior: BOMB_THROW 3マス
→ 投擲距離、爆発遅延、放物線表示等はBehavior
```

同じ `CIRCLE` を「自分中心」「着弾地点」「敵位置」など複数Behaviorから再利用できる設計とする。

## 13. 既存5チップのRange割当

現時点の正式方針は以下。

| チップ | Range Type | Range Parameter |
| --- | --- | --- |
| キャノン | `LINE` | `length_tiles=5`, `width_tiles=0.5` |
| ソード | `RECT` | `length_tiles=1`, `width_tiles=1` |
| ワイドソード | `RECT` | `length_tiles=1`, `width_tiles=3` |
| ミニボム | `CIRCLE` | `radius_tiles=0.75` |
| リカバリー10 | `SELF` | なし |

キャノン・ソード・ワイドソード・ミニボムはマスタ・実行時変換・共通Range形状生成まで移行済み。
ミニボムの投擲距離はRange Parameterではなく `BOMB_THROW.THROW_DISTANCE_TILES=3` とする。
旧 `THROW_AOE` は互換定義として残してよいが、既存5チップの正式マスタでは使用しない。

2026-08-25、キャノン弾の視認性向上に合わせて表示サイズを88×44pxへ拡大し、正式Range幅も `0.25マス` から `0.5マス` へ拡大した。射程5マスは維持する。

## 14. プレビューと判定の共通化

攻撃範囲プレビュー専用の別計算は行わない。

`js/combat/range-geometry.js` が生成したworld座標上のRange形状を、`js/combat/range-preview-renderer.js` が現在の斜め投影へ変換して表示する。

原則:

```text
プレビューが描く形状
= BattleNetworkRangeGeometry が生成した形状
= 敵Hit判定・地形判定が参照する形状
```

これにより、「表示だけ広い／狭い」「Hit判定だけ別形状」となる二重管理を避ける。

キャノン・ソード・ワイドソードは新プレビューへ移行済みで、描画形状は実機確認済み。
初回実装ではRange描画中の操作負荷が大きかったため、v43でSceneサイズ・投影係数のキャッシュ、不要なhide/show抑制、同一属性更新のスキップを実施し、実機で改善を確認済み。

ミニボムは `CIRCLE(radius_tiles=0.75)` を同じ共通Rangeへ接続する。
CIRCLEはworld上の円を24～48頂点のポリゴンへ毎フレーム変換せず、斜め投影後に得られるSVG楕円を直接描画することでプレビュー負荷を抑える。
投擲経路はRangeではなくBehaviorプレビューとして `js/combat/bomb-preview-renderer.js` が3マス先の着弾地点までの放物線を描画する。
またv46ではカスタムゲージ更新とRange描画を分離し、ゲームループ内の二重プレビュー描画を解消した。

v51でRange専用レイヤーへ分離し、実機では稀な引っかかりを残すものの実用上許容まで改善済み。

## 15. 実装順序

本設計は確定済みで、2026-08-25から段階実装中。

次の順で進める。

1. Range形状タイプとRange Parameterを確定する。**完了**
2. 20×20論理グリッド基盤を追加する。**完了**
3. 見た目の床グリッドを論理グリッドと一致させる。**完了／実機確認済み**
4. プレイヤー所属マス取得を実装する。**完了**
5. 攻撃距離をマス単位からワールド距離へ変換する共通処理を追加する。**完了**
6. キャノン・ソード・ワイドソードを新Range方式へ移行する。**完了／実機確認済み**
7. ミニボムの `radius_tiles` を実機比較して確定する。**完了／0.75採用**
8. ミニボムを `CIRCLE / RADIUS_TILES` へ完全移行し、投擲距離3マス・放物線表示へ変更する。**完了／実機確認済み**
9. 敵HitBox導入時に `BattleNetworkRangeGeometry` を正式当たり判定へ接続する。**完了／実機確認済み**
10. 特殊地形は基礎戦闘成立後または必要なチップ実装時に追加する。

既存の自由移動・カメラ・チップ操作を壊さず、小さい単位で実装と実機確認を行う。