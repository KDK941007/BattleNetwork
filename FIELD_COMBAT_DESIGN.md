# BattleNetwork フィールド・攻撃範囲設計

最終更新: 2026-08-24
Status: Design Confirmed / Implementation Pending

このドキュメントは、戦闘フィールドの論理マス、プレイヤーとマスの関係、バトルチップの攻撃範囲判定に関する Source of Truth とする。
フィールド描画、地形効果、移動衝突、攻撃範囲を実装・変更する場合は本設計を基準にする。

## 1. 基本思想

BattleNetworkは「マス制移動ゲーム」にはしない。

- プレイヤー移動は360度自由移動を維持する。
- プレイヤーの向きも360度自由方向を維持する。
- フィールド内部には論理マスを定義する。
- マスは移動を固定するためではなく、地形・攻撃距離・地形変更等のゲーム世界共通単位として利用する。
- 見えているマスと内部の論理マスは1対1で一致させる。

目的は以下。

1. マグマ、毒、穴等の特殊地形をマス単位で管理する。
2. バトルチップの射程・幅を「何マス分」として定義できるようにする。
3. 自由移動・360度攻撃と、地形の離散的な管理を両立する。

## 2. 論理フィールド

現在のゲームワールドは `3600 × 3600 world units` の正方形である。

正式な論理グリッドは以下とする。

- `TILE_SIZE = 180 world units`
- `GRID_COLS = 20`
- `GRID_ROWS = 20`
- 合計400マス

`3600 / 180 = 20` となるため、フィールド端に半端なマスを作らない。

各マスは `row / col` で識別する。

```text
row: 0 - 19
col: 0 - 19
```

ワールド座標から所属マスを求める基本式は以下。

```text
col = floor(worldX / TILE_SIZE)
row = floor(worldY / TILE_SIZE)
```

フィールド端では0～19へクランプする。

## 3. 描画と論理マス

現在の床に表示されている繰り返し模様は装飾であり、論理マスではない。

論理グリッド実装時は、現在の装飾グリッドをそのまま判定へ流用せず、20×20の論理マスと一致する床表示へ置き換える。

原則:

```text
画面に見える1マス = 内部データ上の1マス
```

斜め上視点は表示上の投影であり、論理フィールド自体は180×180の正方形マスとして扱う。
端末解像度・カメラ位置・カメラ倍率によって論理マスサイズを変えない。

## 4. プレイヤーとマス

### 4.1 自由移動

プレイヤーはマス中央へスナップさせない。
現在の360度自由移動を維持する。

### 4.2 所属マス

プレイヤーの所属マスは、原則として「足元中央点」が入っている1マスとする。

床効果の基本判定は所属マスを使用する。

例:

- 毒床
- マグマ床
- 草地
- 氷床等の床効果

キャラクター画像の一部が隣マスへ重なっただけでは所属マスを変更しない。

### 4.3 移動衝突

所属マス判定と移動衝突判定は分離する。

- 床効果: 足元中央点による所属マス判定。
- 穴・侵入不可地形等: プレイヤーの移動用HitBoxによる面判定。

侵入不可地形へ斜めに接触した場合は、可能であればX/Y方向を分けて判定し、完全停止ではなく地形に沿って滑れる移動を目標とする。

## 5. 地形データ

マスは将来的に以下のような地形を持てる構造とする。

初期想定:

- `NORMAL`
- `POISON`
- `MAGMA`
- `HOLE`
- `ICE`
- `GRASS`

ただし、全種類を最初から実装する必要はない。

最低限、マスごとに以下を表現できる設計とする。

```text
row
col
terrain
walkable
```

一時的な地形変更を考慮し、将来的には元地形と現在地形を分離可能な構造とする。

例:

```text
baseTerrain
currentTerrain
duration
```

具体的な地形効果・持続時間・属性相互作用は別途設計する。

## 6. 攻撃範囲の基本思想

攻撃方向を東西南北や8方向へスナップさせない。

バトルチップは、プレイヤーが向いている360度自由方向をそのまま攻撃方向として使用する。

一方、射程や幅の設計単位には論理マスを使用する。

つまり、

```text
方向 = 連続値（自由角度）
距離・幅 = マス数
実際の当たり判定 = 連続ワールド座標
```

とする。

「マスを攻撃先の固定セルとして使う」のではなく、「ゲーム世界の距離単位として使う」。

## 7. マス数からワールド距離への変換

Rangeマスタでは、原則として射程・幅・半径等をマス単位で表現する。

実行時に以下でワールド距離へ変換する。

```text
worldDistance = tileDistance × TILE_SIZE
```

`TILE_SIZE = 180`。

例:

```text
キャノン
射程: 5マス
→ 5 × 180 = 900 world units
```

既存プロトタイプのキャノン射程900と一致する。

ソード等の既存px値はプロトタイプ値であり、論理グリッド移行時にマス単位へ再定義する。

## 8. 360度攻撃範囲

攻撃開始時にプレイヤーの向きベクトルを取得する。

```text
direction = normalized(playerFacing)
```

例えば射程5マスなら、攻撃終点は概念上以下となる。

```text
endX = playerX + directionX × (5 × TILE_SIZE)
endY = playerY + directionY × (5 × TILE_SIZE)
```

プレイヤーが斜めを向いていても、その角度のまま範囲を回転させる。

攻撃開始後にスティック入力が変わっても、その攻撃の方向は途中で回転させない。
A押下時点の向きをその攻撃の基準方向として固定する。

## 9. 敵への当たり判定

敵への攻撃判定は「敵がどのマスにいるか」だけでは判定しない。

敵HitBoxと、回転済みの攻撃範囲を連続ワールド座標上で判定する。

これにより、自由移動中の敵・プレイヤーに対して自然な360度アクションの当たり判定を維持する。

マスは射程・幅等の設計単位として使用し、敵Hit判定そのものを離散化しない。

## 10. 地形への攻撃判定

地形変更系のチップでは、360度自由方向の攻撃範囲と論理マスを組み合わせる。

基本処理:

1. プレイヤーの向きから連続座標上の攻撃範囲を生成する。
2. 20×20の論理マスを対象に範囲との関係を判定する。
3. 対象となったマスへ地形変更を適用する。

範囲の端がマスへわずかに触れただけで対象が不安定に増減することを避けるため、地形変更対象マスは原則として「マス中心点が攻撃範囲内に入っているか」で判定する。

この中心点ルールは地形変更に対する基本方針とし、特殊チップで別ルールが必要になった場合のみ個別定義する。

## 11. Range形状

Rangeの具体的な形状タイプは次の設計工程で確定する。

候補例:

- 前方直線
- 前方長方形
- 円形
- 扇形
- 投擲＋着弾地点円形
- 自分中心
- ドーナツ型等

`M_RANGE_TYPE` が形状を表し、チップごとの射程・幅・半径等はRange Parameterとして保持する方針は維持する。

具体的なType名、必須Parameter、既存5チップの割当は次工程で決定する。

## 12. 実装順序

本設計は確定済みだが、2026-08-24時点では未実装。

次の順で段階実装する。

1. Range形状タイプとRange Parameterを確定する。
2. 20×20論理グリッド基盤を追加する。
3. 見た目の床グリッドを論理グリッドと一致させる。
4. プレイヤー所属マス取得を実装する。
5. 攻撃距離をマス単位からワールド距離へ変換する共通処理を追加する。
6. 既存5チップの範囲を新方式へ移行する。
7. 敵HitBox導入時に連続座標の正式当たり判定へ接続する。
8. 特殊地形は基礎戦闘成立後または必要なチップ実装時に追加する。

既存の自由移動・カメラ・チップ操作を壊さず、小さい単位で実装と実機確認を行う。
