# BattleNetwork ココロ・善悪度・ダークチップ仕様

更新日: 2026-09-09

## 1. このドキュメントの位置づけ

本書は、バトルシステムのうち以下の確定仕様を管理する。

- ココロウィンドウ
- ココロ値
- 善悪度
- ダークチップ
- ソウルクリーナー

原作要素を参考にするが、本プロジェクトで会話上確定した仕様を優先する。
未確定事項は推測で補完せず、末尾の「未確定事項」に分離する。

本仕様におけるバトルの判定単位は **1ウェーブ** とする。

---

## 2. ココロウィンドウの種類

採用するココロ状態は以下の5種類とする。

1. 平常
2. フルシンクロ
3. 不安
4. 怒り
5. 悪

平常の内部状態IDは既存実装に合わせて `NORMAL` とする。
その他状態の内部IDは未確定とする。

---

## 3. ココロ値

### 3.1 基本

ココロ値を採用し、ココロ状態の基本判定に使用する。

| ココロ値 | 状態 |
| ---: | --- |
| 255 | フルシンクロ |
| 65〜254 | 平常 |
| 1〜64 | 不安 |
| 0 | 悪の特殊領域 |

ただし、善悪度またはダークチップ使用によって「悪固定」となっている場合は、通常のココロ値による状態遷移を行わない。

### 3.2 ココロ値の増減

- 攻撃チップでカウンターに成功した場合、ココロ値を `255` にする。
- バスターによるカウンターではフルシンクロへ移行しない。
- 暗転しない攻撃チップを敵へヒットさせた場合、そのチップに設定された「ココロ攻撃力」をココロ値へ加算する。
- 多段攻撃は1ヒットごとにココロ値を加算する。
- 複数の敵へ同時にヒットした場合は、命中した敵ごとに加算する。
- 敵の攻撃を受けた場合、その攻撃に設定されたココロ攻撃力をココロ値から減算する。
- リカバリー系による上昇では `254` を上限とし、リカバリーだけではフルシンクロへ到達しない。
- フルシンクロ中に攻撃力2倍を適用した攻撃チップを使用した後、ココロ値を `153` にする。
- 怒り発生時はココロ値を `128` にする。
- 怒り中はココロ値を変動させない。
- ソウルユニゾン解除時はココロ値を `128` にする。
- ソウルユニゾン中はココロ値を変動させない。

攻撃力とココロ攻撃力は別パラメータとして扱う。

---

## 4. 平常

- 基準状態とする。
- 攻撃力補正なし。
- 防御、移動、のけぞり等への補正なし。
- 特殊効果なし。
- ココロ値が別状態の条件を満たした場合、その状態へ遷移する。

---

## 5. フルシンクロ

### 5.1 発生

以下のいずれかで発生する。

- 攻撃チップでカウンター成功し、ココロ値が255になる。
- 非暗転攻撃チップの命中等によってココロ値が255へ到達する。

バスターによるカウンターは対象外とする。

### 5.2 効果

- 次に使用する攻撃チップの攻撃力を **2倍** にする。
- 攻撃力を持たないチップを使用しても2倍効果を消費しない。
- 多段攻撃は全ヒットを2倍にする。
  - 例: `10×3 → 20×3`
- アタック+10等の攻撃力加算が付いている場合、**加算後の攻撃力を2倍** にする。
  - 例: `(80 + 10) × 2 = 180`

2倍効果を適用した攻撃チップ使用後はココロ値153へ戻し、通常のココロ値判定へ復帰する。

---

## 6. 不安

### 6.1 発生・解除

- ココロ値 `1〜64` で不安状態とする。
- ココロ値が `65以上` へ戻った場合、不安を解除する。
- 攻撃チップでカウンターに成功してココロ値255になった場合は、そのままフルシンクロへ移行する。

以前検討した以下の独自条件は採用しない。

- 5秒以内に3回被弾
- 1回で最大HPの1/3以上のダメージ
- 5秒間ノーダメージで解除

### 6.2 効果

- ソウルユニゾン不可。
- フォルダ内に未使用のダークチップが残っている場合、次回CUSTOM画面でダークチップを最低1枚表示する。
- 通常抽選ですでにダークチップが表示されている場合は、追加保証しない。
- 候補となるダークチップが複数ある場合、保証対象の1枚は候補からランダムに選ぶ。
- フォルダに入っていないダークチップをゲーム側でランダム生成しない。

---

## 7. 怒り

### 7.1 発生

敵攻撃による、のけぞり・麻痺等の操作不能状態が **連続2秒以上** 続いた場合に発生する。

怒り発生時、ココロ値を `128` にする。

### 7.2 効果

- スーパーアーマー状態とし、怒り中は被弾によるのけぞりを発生させない。
- 次に使用する攻撃チップの攻撃力を **2倍** にする。
- 攻撃力を持たないチップでは2倍効果を消費しない。
- 多段攻撃は全ヒットを2倍にする。
- アタック+10等の攻撃力加算は、加算後の攻撃力を2倍にする。
- 怒り中はココロ値を変動させない。

### 7.3 解除

以下のいずれかで解除する。

- 2倍効果を適用した攻撃チップを使用する。
- 怒り発生から **5秒** 経過する。

解除後はココロ値128を基準として通常のココロ状態判定へ戻る。

---

## 8. 善悪度

### 8.1 基本

善悪度はウェーブをまたいで保存する長期パラメータとする。

- 初期値: `500`
- 上限: `1000`
- 下限: `0`

| 善悪度 | 区分 |
| ---: | --- |
| 1000 | 善 |
| 500〜999 | 通常 |
| 470〜499 | やや悪 |
| 301〜469 | 悪 |
| 0〜300 | 完全な悪 |

### 8.2 ウェーブ開始時のココロ値

- 善悪度 `1000` の場合: 開始ココロ値 `190`
- 善悪度 `470〜999` の場合: `128 + floor(善悪度 / 20)`
- 善悪度 `0〜469` の場合: ウェーブ開始時から悪状態で固定し、通常のココロ値による平常・不安・フルシンクロ等への遷移を行わない。

例:

- 善悪度500 → 開始ココロ値153
- 善悪度800 → 開始ココロ値168
- 善悪度999 → 開始ココロ値177
- 善悪度1000 → 開始ココロ値190

### 8.3 善悪度の回復

- 善悪度301以上で、ダークチップを使用せずにウェーブへ勝利した場合、善悪度を回復可能とする。
- 基本回復量は `+1` とする。
- 強制戦闘・逃走不可戦闘に相当するウェーブの回復量は `+3` とする。
- 善悪度300以下では、ソウルクリーナーなしの通常勝利では善悪度を回復できない。

強制戦闘・逃走不可戦闘をゲーム上どのウェーブへ設定するかは未確定とする。

### 8.4 ダークチップ使用時の善悪度低下

1ウェーブ中にダークチップを1枚以上使用した場合、ウェーブ終了時に1回だけ以下を適用する。

| 使用前の善悪度 | ウェーブ終了時の処理 |
| ---: | --- |
| 500〜1000 | 480へ変更 |
| 470〜499 | -4 |
| 1〜469 | -1 |
| 0 | 0のまま |

同一ウェーブでダークチップを複数枚使用しても、善悪度低下判定は1回のみとする。

---

## 9. 悪状態

### 9.1 発生

以下の場合に悪状態となる。

- 善悪度469以下でウェーブを開始した場合。
- ウェーブ中にダークチップを1枚以上使用した場合。

ダークチップ使用によって悪になった場合、そのウェーブ終了まで悪状態で固定する。

### 9.2 効果

- フルシンクロへ移行しない。
- 不安へ移行しない。
- 怒りへ移行しない。
- ソウルユニゾン不可。
- フォルダ内に未使用のダークチップが残っている場合、次回CUSTOM画面で最低1枚を表示する。
- 通常抽選ですでにダークチップが表示されている場合は追加保証しない。
- 候補が複数ある場合、保証対象の1枚をランダムに選ぶ。

ダークチップ使用による悪固定が解除されるのはウェーブ終了時とする。
次ウェーブの開始状態は、その時点の善悪度から改めて判定する。

---

## 10. 善悪度によるチップ使用制限

- 通常チップ: 善悪度に関係なく使用可能。
- 善専用チップ: 善悪度 `470以上` で使用可能。
- 悪専用チップ: 善悪度 `469以下` で使用可能。
- ダークチップ: 上記の善専用／悪専用とは別枠として扱う。

どのチップを善専用・悪専用へ分類するかは未確定とする。

---

## 11. ダークチップ

### 11.1 基本方針

ダークチップの所持・フォルダ・CUSTOM出現方式はEXE5方式をベースにする。

- ダークチップは実物のチップとして所持する。
- プレイヤー自身がフォルダへ編成する。
- フォルダに入れていないダークチップをランダム生成しない。
- フォルダ内のダークチップ上限は **5枚** とする。
- 同名ダークチップは **1枚まで** とする。

### 11.2 CUSTOM画面での出現

- 平常 / フルシンクロ / 怒り: 通常チップと同じ抽選。特別な出現補正なし。
- 不安 / 悪: フォルダ内に未使用ダークチップが残っていれば最低1枚を保証表示する。
- 通常抽選ですでに1枚以上表示されている場合、追加保証しない。
- 保証候補が複数ある場合はランダムで1枚選ぶ。

### 11.3 使用時の共通ペナルティ

1ウェーブ中にダークチップを1枚以上使用した場合:

- 使用した時点からウェーブ終了まで悪状態で固定する。
- ソウルユニゾン不可。
- ウェーブ終了時に善悪度低下を1回適用する。
- ウェーブ終了時に最大HPを `1` 減少させる。
- 同一ウェーブで複数枚使用しても最大HP減少は `-1` のみとする。

各ダークチップ固有の使用後バグは、チップごとに別途定義する。

### 11.4 採用するダークチップ

EXE5のダークチップ12種類を基本ラインナップとしてすべて採用する。

1. ダークソード
2. ダークトルネード
3. ダークサークル
4. ダークメテオ
5. ダークサンダー
6. ダークリカバリー
7. ダークサウンド
8. ダークインビジ
9. ダークプラス
10. ダークランス
11. ダークドリル
12. ダークワイド

各チップの攻撃力、範囲、固有バグ等は個別検討で確定する。

### 11.5 ダークソード

- 系統: ソード系
- 攻撃力: `400`
- 攻撃範囲: 前方2マス × 幅3マス
- 使用後バグ: バスターが `25%` の確率で空撃ちする。
- 使用後バグの継続: ダークソードを使用したウェーブの終了まで。
- ダークチップ共通ペナルティは11.3の仕様を適用する。

### 11.6 ダークトルネード

- 系統: 風系統
- 攻撃力: `50 × 最大8ヒット`
- 基本攻撃位置: プレイヤーの2マス前を基準に竜巻を発生させる。
- 攻撃範囲: そのウェーブ中にプレイヤーがのけぞった回数に応じて段階的に拡大する。
  - のけぞり0回: 2マス前の1マス。
  - のけぞり1回: 基本位置から横方向へ範囲を拡大する。
  - のけぞり2回: さらに縦方向へ範囲を拡大する。
  - のけぞり3回以上: 最大範囲とする。
- 使用後バグ: CUSTOM画面を開くたびに現在HPへ `100` ダメージを受ける。
- 使用後バグの継続: ダークトルネードを使用したウェーブの終了まで。
- ダークチップ共通ペナルティは11.3の仕様を適用する。

### 11.7 ダークサークル

- 攻撃力: `300`
- 敵側エリアという概念は新設しない。
- 攻撃範囲は、プレイヤーの向きを基準として **前方固定** で配置する。
- 前方に3×3のリング状範囲を展開し、中央マスは攻撃候補から除外する。
- 外周8マスをカーソルが周回する。
- A入力でカーソルを停止し、停止した1マスを安全地帯として残す。
- 残りの外周7マスを攻撃対象とする。
- リングの位置は発動時のプレイヤーの向きを基準に決定し、発動後は固定する。
- リング中心はプレイヤーから **前方3マス** に置く。
- 使用後バグ: CUSTOM画面を開くたびに現在HPへ `100` ダメージを受ける。
- 使用後バグの継続: ダークサークルを使用したウェーブの終了まで。
- 原作の命中相手側へ付与されるCUSTOM画面由来のHPバグは、本作の通常戦では適用しない。
- ダークチップ共通ペナルティは11.3の仕様を適用する。

### 11.8 ダークメテオ（確定済み部分）

- 攻撃力: `100 / 1発`
- 発動時に最も近い生存敵1体をロックする。
- 発動中は、残っているメテオがロック対象を追尾する。
- ロック対象が全弾着弾前に撃破された場合、残りのメテオは別の生存敵へターゲットを切り替えて攻撃を継続する。
- 切り替え時点で最も近い生存敵を新たなロック対象とする。
- そのウェーブ中にプレイヤー自身がのけぞった回数に応じて、発射するメテオ数を増加させる。
  - のけぞり0回: `10発`
  - のけぞり1回: `12発`
  - のけぞり2回: `14発`
  - のけぞり3回以上: `16発`
- メテオが着弾したパネルを破壊状態にする。
- 使用後バグ: CUSTOM画面を開いている間、使用者の現在HPが継続的に減少する。
- CUSTOM画面を開いている間、`1秒ごと` に現在HPを `1` 減少させる。
- 通常戦では、敵側へ同種のCUSTOM画面由来HP減少バグを付与しない。
- 使用後バグの継続: ダークメテオを使用したウェーブの終了まで。

### 11.9 ダークサンダー（確定済み部分）

- 属性: 電気属性
- 攻撃力: `200`
- 命中時、対象を麻痺状態にする。
- 麻痺時間は `1.5秒` とする。
- 本作では麻痺の継続時間を共通で `1.5秒` とし、ダークサンダー以外の麻痺付与攻撃にも同じ時間を適用する。
- 発動時に最も近い生存敵1体をロックし、その敵を追尾する。
- 追尾中にロック対象が撃破された場合、その時点で最も近い別の生存敵へロックを切り替え、追尾を継続する。
- 追尾弾は低速で移動し、長時間存続して追尾を続ける。
- 追尾速度は **1秒で約1マス** とする。
- 追尾弾は発射から `10秒` 経過しても命中しない場合、消滅する。
- 攻撃対象はロックした敵1体のみとし、周囲の敵や隣接パネルへの巻き込みダメージは発生しない。
- 使用後バグ: CUSTOM画面を開いている間、使用者の現在HPを減少させる。
- CUSTOM画面を開いている間、`1秒ごと` に現在HPを `1` 減少させる。
- 使用後バグの継続: ダークサンダーを使用したウェーブの終了まで。

### 11.10 ダークリカバリー（確定済み部分）

- 回復量: `1000`
- 回復は現在の最大HPを上限とし、最大HPを超えて回復しない。
- 使用後バグ: バトル中、使用者の現在HPが継続的に減少する。
- HP減少はダークリカバリー使用直後から開始する。
- `1秒ごと` に現在HPを `6` 減少させる。
- 使用後バグの継続: ダークリカバリーを使用したウェーブの終了まで。

### 11.11 ダークサウンド（確定済み部分）

- 設置位置: 使用者の1マス前。
- 設置物の耐久力: `100`
- 効果範囲: 設置されたダークサウンドを中心とする `11×11マス`。
- 設置物が演奏を開始するたび、その時点で効果範囲内にいる敵へ1回だけ `1.5秒` の麻痺を付与する。
- 1回の演奏時間は `3秒` とする。
- 演奏終了後は `2秒` 待機し、その後次の演奏を開始する。
- 設置から `40秒` 経過した場合、耐久力が残っていても設置物は消滅する。
- 設置物の耐久力が `0` になった場合も、その時点で消滅する。
- 使用後バグ: 通常移動の方向入力時、入力した方向の自陣内の端まで瞬間移動する。
- 移動途中のマスには停止しない。
- 使用後バグの継続: ダークサウンドを使用したウェーブの終了まで。

### 11.12 ダークインビジ（確定済み部分）

- 使用時、使用者は `10秒間` ダークソウル状態になる。
- ダークソウル状態中にCUSTOM画面を開いている時間は、`10秒` の継続時間へ含めない。
- CUSTOM画面を開いている間はダークソウルの残り時間カウントを停止し、CUSTOM画面を閉じた後に停止時点の残り時間から再開する。
- ダークインビジを使用した時点で、11.3のダークチップ共通ペナルティの対象とする。
- ダークソウル状態中は、プレイヤーによる通常移動・ダッシュ・攻撃操作を無効とする。
- ダークソウル状態中の移動および戦闘操作はAIが自動で行う。
- AIの各行動判定では、`13/16` の確率でバスター、`3/16` の確率でチップ使用を選択する。
- バスターを選択した場合は、バスターを `3連射` する。
- バスター行動開始時に生存敵からターゲット1体を選択する。
- バスターのターゲットは、現在地から縦方向の位置合わせに必要な移動量が最も少ない生存敵を優先する。
- 選択したターゲットへ照準を合わせられる有効位置へ瞬間移動し、同じターゲットへバスターを `3連射` する。
- バスター3連射の途中ではターゲットを切り替えない。
- AIが使用するチップ候補は、そのウェーブ開始以前の使用履歴を基準にGBA版EXE5方式でカテゴリ別に選出する。
- CUSTOM画面で選択済みのチップが1枚以上残っている場合、AIのチップ使用行動では使用履歴候補よりもCUSTOM選択チップを優先する。
- CUSTOM選択チップは、CUSTOM画面で選択した順に1枚ずつ使用する。
- AIがCUSTOM選択チップを使用した場合、そのチップは通常どおり消費し、CUSTOM側の残りチップ状態へ反映する。
- CUSTOM選択チップをすべて消費した後は、そのウェーブ開始以前の使用履歴を基準とする候補プールからチップを独立再現する。
- 使用履歴から独立再現したチップはCUSTOM側のチップを消費せず、CUSTOM側の選択状態・残りチップ状態を変更しない。
- スタンダードチップおよびダークチップは、使用回数上位 `16種類` を候補とする。
  - 使用回数1位・2位は、それぞれ候補プールへ `4枠` 登録する。
  - 使用回数3位・4位は、それぞれ候補プールへ `2枠` 登録する。
  - 使用回数5位〜16位は、それぞれ候補プールへ `1枠` 登録する。
- メガクラスチップは、使用回数上位 `5種類` を候補とし、それぞれ候補プールへ `1枠` 登録する。
- ギガクラスチップは、使用回数上位 `1種類` を候補とし、候補プールへ `1枠` 登録する。
- プログラムアドバンスは、使用回数上位 `1種類` を候補とし、候補プールへ `1枠` 登録する。
- 暗転チップも使用候補に含める。
- 使用履歴から有効なチップ候補を1件も作成できない場合、代替チップを生成・補充しない。
- CUSTOM選択チップが残っている間は、使用履歴候補が0件でも通常の `13/16` バスター・`3/16` チップ使用の行動抽選を継続し、チップ使用時はCUSTOM選択チップを優先する。
- CUSTOM選択チップをすべて消費した後、かつ使用履歴候補が0件の場合は、そのダークソウル状態中はチップ使用抽選を行わず、バスターのみで行動する。
- 選択中チップがない状態でチップ使用が選ばれた場合、AIは上記の枠数を重みとして候補プールからランダムにチップを1つ選択し、選択中チップとして保持する。
- 選択中チップが暗転チップであり、チップ使用失敗回数が `0` の場合は、その場ではチップを発動しない。
- この場合、チップ使用失敗回数を `1` 増加させ、使用可能なパネルからランダムに1マスを選んで瞬間移動する。
- 暗転チップの初回失敗後も選択中チップは保持し、次回以降の行動判定は通常どおり `13/16` バスター・`3/16` チップ使用で行う。
- その後チップ使用が選ばれた時点で、選択中の暗転チップがあり、チップ使用失敗回数が `1以上` であれば、その暗転チップの使用を試行する。
- チップまたはコンボの使用に成功した場合、チップ使用失敗回数を `0` に戻す。
- チップ使用失敗回数が `5` に達した場合、選択中チップを破棄し、候補プールから別のチップをランダムに再抽選する。
- チップ使用失敗回数および選択中チップは、ダークソウル状態中のみ管理する。
- ダークソウルAIがダークチップを使用しても、11.3のダークチップ共通ペナルティを追加でカウントしない。
- ダークソウルAIが使用したダークチップでは、そのチップ固有の使用後バグを発生させない。
- ダークソウルAIが使用したダークチップの攻撃・回復等の本来効果は通常どおり発生する。
- 使用後バグ: ダークソウル終了後、通常移動の方向入力時に入力した方向の自陣内の端まで瞬間移動する。
- 移動途中のマスには停止しない。
- 使用後バグの継続: ダークインビジを使用したウェーブの終了まで。
- この使用後バグはダークサウンドおよびダークプラスと同一仕様とする。
- AIは攻撃を実行するたび、その時点で生存している敵から攻撃対象を再選択する。
- AIは攻撃対象と使用チップを決定した後、そのチップで対象を攻撃できる有効位置のうち、現在地から最も近い位置へ移動する。
- 現在地ですでに対象を攻撃可能な場合は、攻撃位置への移動を行わない。
- 攻撃位置への移動は、現在地から目的マスへ直接移る瞬間移動とする。
- この瞬間移動はダッシュとは別扱いとし、ダッシュ回数を消費せず、ダッシュのクールダウンも発生させない。
- 瞬間移動では途中のマスを通過せず、現在地から目的マスへ直接移動する。
- 瞬間移動先として使用できないマス（穴・敵・設置物等）は候補から除外する。
- 該当位置が使用不能な場合、同じチップで対象を攻撃できる別の有効位置のうち、現在地から最も近い位置を選ぶ。
- 攻撃可能な有効位置が1つもない場合は、そのチップの使用を見送り、選択中チップを保持する。
- 攻撃可能な有効位置がないことでは、チップ使用失敗回数を増加させない。
- 次の行動判定は通常どおり `13/16` バスター・`3/16` チップ使用で行う。
- 次にチップ使用が選ばれた場合、その時点の生存敵から攻撃対象を再選択し、保持している同じ選択中チップの使用を再試行する。
- ダークソウル状態中は、攻撃によるダメージ・状態異常・のけぞり等の攻撃由来効果を原則無効化する。
- `インビジ貫通`として扱う攻撃であっても、ダークソウル状態の無敵は貫通しない。
- 攻撃ダメージではなくHPを直接変動させる効果等、ダークソウル状態中にも有効とする特殊効果は、個別仕様で明示したものだけ例外とする。
- 個別仕様で例外指定していない特殊ギミックは、ダークソウル状態の無敵を貫通しない。

### 11.13 ダークプラス（確定済み部分）

- 効果: ダークプラス使用後、次に使用するチップの付加攻撃力を `+50` とする。
- 次に使用するチップへアタック+10等の付加攻撃力が設定されている場合、それらへ `+50` を加算するのではなく、付加攻撃力を `+50` へ上書きする。
- 次に使用したチップが攻撃系チップではない場合、ダークプラスの `+50` 効果は不発として消滅する。
- 不発となった `+50` 効果は、その後に使用する攻撃系チップへ持ち越さない。
- 使用後バグ: 通常移動の方向入力時、入力した方向の自陣内の端まで瞬間移動する。
- 移動途中のマスには停止しない。
- 使用後バグの継続: ダークプラスを使用したウェーブの終了まで。
- この使用後バグはダークサウンドおよびダークインビジと同一仕様とする。
- ダークプラスのその他の効果・数値仕様は未確定とする。

### 11.14 ダークランス（確定済み部分）

- 属性: 木属性。
- 攻撃力: `400`。
- 攻撃位置・範囲の判定は、発動時のプレイヤーの向きを基準とする。
- 敵探索範囲は、プレイヤー正面の `4〜6マス先` とし、正面軸から左右へ `2マスずつ` 広げた **横5マス × 奥行3マス（5×3）** とする。
- 上記5×3の探索範囲内にいる生存敵のうち、プレイヤーから最も奥にいる敵の列を攻撃列として決定する。
- 攻撃範囲は、決定した攻撃列の **横5マスすべて** とする。
- 同じ最奥距離に複数の敵がいる場合も、攻撃列は同一であるため、その列の横5マスすべてを攻撃する。
- 5×3の探索範囲内に生存敵が1体もいない場合は、探索範囲の最奥列であるプレイヤー正面 `6マス先` の横5マスを攻撃する。
- 命中した敵は、使用者側へ `1マス` 押し出す。
- 障害物は押し出さない。
- ダークランス自体では、命中した敵へののけぞりを発生させない。
- 使用後バグ: ダークランス使用後、使用者が敵攻撃を受けるたびに混乱状態になる。
- 混乱の継続時間は、後で定義する本作共通の混乱状態仕様を使用する。
- 通常戦では、ダークランス命中相手へ「被弾時に混乱する」同種バグを付与しない。
- 使用後バグの継続: ダークランスを使用したウェーブの終了まで。

### 11.15 ダークドリル（確定済み部分）

- 系統: ブレイク系統。
- 攻撃力: `100 / 1ヒット`。
- 攻撃範囲: 発動時のプレイヤーの向きを基準とする前方 `1〜2マス`、横幅 `1マス`。
- 基本攻撃回数は `6回` とする。
- 前方1マスの敵に命中し、初回命中で前方2マスへ押し出されることで根元側と先端側の攻撃判定をまたいだ場合、最大 `7ヒット`（合計 `700` ダメージ）まで命中する。
- ブレイク性能あり。
- 初回命中時、命中した敵を使用者から離れる方向へ `1マス` 押し出す。
- 押し出し先が使用不能な場合は押し出さず、その場に敵を残す。
- 押し出しが発生しない場合も、敵がダークドリルの攻撃マス上にいる限り残りのヒット判定を継続する。
- 使用後バグ: 使用者が移動した際、移動前のパネルを `1/2` の確率で毒沼パネルへ変化させる。
- 通常戦では、ダークドリル命中相手へ同種の移動時毒沼化バグを付与しない。
- 使用後バグの継続: ダークドリルを使用したウェーブの終了まで。

### 11.16 ダークワイド（確定済み部分）

- 属性: 水属性。
- 攻撃力: `300`。
- チップコード: `T`。
- 攻撃は発動時のプレイヤーの向きを基準に、正面方向へ幅 `3マス` のワイドショットとして発射する。
- 射程: プレイヤー正面方向へ `7マス`。
- 攻撃範囲: 幅 `3マス` × 奥行 `7マス`。
- 弾は敵を貫通し、命中しても消滅しない。
- 弾は障害物を貫通し、障害物に命中しても消滅しない。
- 破壊可能な障害物には `300` ダメージを与えたうえで貫通する。
- 破壊不能な障害物も弾を遮らない。
- 1回のダークワイドにつき、同じ敵へのダメージ判定は `1回` のみとする。
- 複数の敵を貫通した場合、それぞれの敵へ1回ずつ `300` ダメージを与える。
- 使用後バグ: バスターが `25%` の確率で空撃ちする。
- 通常戦では、ダークワイド命中相手へ同種のバスター空撃ちバグを付与しない。
- 使用後バグの継続: ダークワイドを使用したウェーブの終了まで。

---

## 12. ソウルクリーナー

ソウルクリーナーは **ナビカスプログラム** として採用する。

### 12.1 善悪度回復

- 善悪度300以下であっても、ソウルクリーナー装備中は善悪度を回復可能とする。
- ダークチップを使用せずに通常ウェーブへ勝利: `+1`
- ダークチップを使用せずに強制戦闘・逃走不可ウェーブへ勝利: `+3`

### 12.2 最大HP回復

- ソウルクリーナー装備中に、ダークチップを使用せず **100ウェーブ勝利** するごとに、ダークチップ使用によって失った最大HPを `1` 回復する。
- 100勝到達後は次の100勝を再カウントする。
- 最大HPは本来の最大HP上限を超えて回復しない。
- ダークチップ以外の要因による最大HP変化は、この回復対象へ含めない。

---

## 13. ソウルユニゾン（共通仕様）

- ソウルユニゾンはCUSTOM画面で発動する。
- プレイヤーが取得済みのソウルのみ使用できる。
- 対応系統のチップを1枚選択し、ユニゾン用の素材チップとして消費する。
- レギュラーチップはソウルユニゾンの素材に使用できない。
- 基本継続時間は `3ターン` とする。
- 本作では、CUSTOM画面を閉じてから次にCUSTOM画面を開くまでのAction区間を `1ターン` として数える。
- 同じソウルへのユニゾンは `1ウェーブにつき1回まで` とする。
- ソウルユニゾン中でも、まだそのウェーブで使用していない別のソウルへユニゾンできる。
- 別のソウルへユニゾンした場合、現在のソウルを解除し、新しいソウルへ上書きする。前のソウルの残りターンは破棄する。
- 平常・フルシンクロ・怒り状態ではソウルユニゾンを使用できる。
- 不安状態および悪状態ではソウルユニゾンを使用できない。
- ソウルユニゾン中はココロ値を変動させない。
- ソウルユニゾン解除時はココロ値を `128` にする。
- 通常のソウルユニゾン中にダークチップを使用した場合、先に現在のソウルユニゾンを解除し、その後ダークチップを発動する。
- 上記ダークチップ使用後は11.3の共通ペナルティに従って悪状態で固定されるため、そのウェーブ中は再びソウルユニゾンできない。
- 通常のソウルユニゾン中は、ダークチップをソウル能力による溜め撃ちとして使用できない。
- ダークチップそのものを溜め撃ちとして使用する仕組みは、カオスユニゾンとして別途定義する。

### 13.1 ナンバーソウル（確定済み部分）

- 属性: 無属性。
- ソウルユニゾン素材: 数値付加系チップ `1枚`。
- ナンバーソウル中はCUSTOM画面のチップ表示枚数を `10枚` とする。
- 無属性かつ攻撃力を持つチップは、1ヒットあたりの攻撃力へ `+10` する。
- 上記 `+10` は暗転・非暗転を問わず適用する。
- 多段攻撃では各ヒットの攻撃力へ `+10` する。
- 攻撃力を持たない無属性チップには `+10` を適用しない。
- ナンバーソウルの `+10` はソウル固有の常時補正として扱い、アタック+10等の付加攻撃力とは別枠で加算する。
- Bボタンの溜め撃ちはサイコロボムとする。
- サイコロボムは発動時のプレイヤーの向きを基準として正面 `3マス` へ投擲する。
- 着弾時、着弾点を中心とする最大 `3×3マス` を攻撃する。
- 爆発範囲のうちフィールド外に出るマスには攻撃判定を発生させない。
- サイコロの出目は `1〜6` からランダムに決定する。
- サイコロボムの1ヒットあたりの攻撃力は `(5 × Buster Attack + 5) × サイコロの出目` とする。
- サイコロ本体が敵へ直接命中した場合、その敵へのダメージ判定は `1回` とする。
- ナンバーソウルの継続時間・解除・再使用制限等は13章の共通仕様を適用する。

---

## 14. 未確定事項

以下は現時点では確定していないため、本書では仕様を固定しない。

- 平常以外のココロ状態の内部ID
- 各プレイヤーチップのココロ攻撃力
- 各敵攻撃のココロ攻撃力
- 善専用／悪専用チップの具体的な分類
- 強制戦闘・逃走不可ウェーブのゲーム上の定義
- ダークサウンドのその他未確定部分
- ダークインビジのダークソウル状態のその他未確定部分
- ダークプラスのその他未確定部分
- ダークランスの混乱状態の共通継続時間
- ナンバーソウル以外の各ソウルの個別能力、カオスユニゾン、ソウルタイム+等の未確定部分
- ナンバーソウルのCUSTOM10枚と、CUSTOM枚数を変更する他要素との優先順位
- ココロウィンドウ各状態の最終イラスト・演出
- バトル報酬仕様

---

## 15. 実装状況

本書は現時点の **設計確定事項の記録** であり、ここに記載したココロ値・善悪度・ダークチップ仕様がすべて実装済みであることを意味しない。

実装は別タスクとして行い、実装・実機確認が完了した項目のみ開発状況ドキュメントへ反映する。